id:         S161
goal:       release 1.0.0 to the config roots
accepts:    master carries the v1 tree at version 1.0.0; Max has pushed; both
            config roots serve 1.0.0 after plugin update (user scope in the
            CLI root, user and project scopes in the desktop root)
touches:    git (merge v1 into master), the installed plugin registries
excludes:   any product change
author:     Maksym Bodnar
done:
