id:         S096
goal:       bump 0.7.0, re-run the audit, close the findings it no longer reports
accepts:    .claude-plugin/plugin.json reads 0.7.0 in its own commit and the golden
            plugin test still passes; the bump is what makes the install path fetch
            new bytes, since the cache directory is keyed by version.
            A fresh adversarial audit runs against the code at that commit through
            the adversarial_reviewer subagent, on a clean context, with
            --audit check exit 0 before anything is acted on.
            Every 2026-08-08_adversarial.4 finding the re-run no longer reports
            moves to closed, each re-run from its own reproduction rather than
            trusted from a step stamp; one that still reproduces stays open with a
            step or a decision rather than a closed stamp.
            New findings are triaged one step each, correctness ordered ahead of
            feature work.
            DEC-035's stopping rule is applied out loud: a re-run with no high and
            no medium ends the audit loop, and this run either qualifies or does
            not, said explicitly.
touches:    .claude-plugin/plugin.json; adocs/audit/; adocs/plan.md and new step
            files; adocs/status.md; MANUAL.md if any version-attributed sentence
            needs to name 0.7.0
excludes:   reinstalling the plugin, which is Max's own (claude plugin install
            moltke@moltke) and was verified live in S059; fixing anything the
            re-run finds, which is the triaged steps' job
decisions:  DEC-034, DEC-035
closes:
blocks:
paused_by:
done:
