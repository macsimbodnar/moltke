id:         S139
goal:       install and verify 0.12.0, the merged plugin
accepts:    `--version` in a live session reports 0.12.0 from the plugin cache; the Monitor matcher refuses a hand-composed persistent watcher and allows the primitive, observed live rather than from a stdin fixture; SessionStart reports watch state; whatever the live session contradicts is recorded
touches:    nothing in the repository; this is a verification step
excludes:   changing behaviour to make the live session agree — that becomes its own step
decisions:  DEC-052
closes:
blocks:
paused_by:
done:
