id:         S171
goal:       release 1.1.0 — the audit fixes ship, tagged
accepts:    plugin.json says 1.1.0; the bump commit is the release commit and
            carries an annotated tag v1.1.0; Max pushes commits and tag; each
            config root present takes the update once master carries it
touches:    .claude-plugin/plugin.json, git tags
excludes:   any product change beyond the version field
author:
done:
