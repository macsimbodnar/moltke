id:         S155
goal:       release 0.13.0 and reinstall it in both config roots
accepts:    `.claude-plugin/plugin.json` reads 0.13.0, the plugin golden test is green, and `--version` reports 0.13.0 from each config root's own cache after an install there, observed live rather than assumed; whether a `directory` source refreshes without a version bump is established and written down; MANUAL's Known issues reflects what shipped and what did not
touches:    .claude-plugin/plugin.json, MANUAL.md
excludes:   fixing anything the release exposes, which becomes its own step
decisions:  DEC-057
closes:
blocks:
paused_by:
done:
