id:         S044
goal:       bump 0.4.0, re-run the audit, close the 2026-08-07 findings
accepts:    plugin.json is 0.4.0; the audit is re-run and each 2026-08-07 finding gets a verdict decided against the code rather than against the step files; every finding that no longer reproduces is closed and every one that does not is said so plainly; --audit list exits 0; status.md regenerated. 2026-08-06_adversarial-F02 is closed here only if a live spawn against an installed 0.3.0 or later re-probes the fence, which is Max's under DEC-014; otherwise it stays planned with the reason restated
touches:    .claude-plugin/plugin.json; adocs/audit/; adocs/status.md
excludes:   fixing anything newly found, which becomes its own step; the live install itself, which is Max's under DEC-014
decisions:  DEC-002
closes:
blocks:
paused_by:
done:
