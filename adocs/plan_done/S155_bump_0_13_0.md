id:         S155
goal:       release 0.13.0 and reinstall it in both config roots
accepts:    `.claude-plugin/plugin.json` reads 0.13.0, the plugin golden test is green, and `--version` reports 0.13.0 from each config root the agent can install into, observed live rather than assumed, with any root it cannot reach named in MANUAL (amended by DEC-061; `~/.claude` takes a release from `origin/master` and the agent never pushes); whether a `directory` source refreshes without a version bump is established and written down; MANUAL's Known issues reflects what shipped and what did not
touches:    .claude-plugin/plugin.json, MANUAL.md, README.md and
            .moltke.local.md — both stated that an edit reaches the hooks
            after `claude plugin update`, which this step disproved
excludes:   fixing anything the release exposes, which becomes its own step
decisions:  DEC-057, DEC-061
closes:
blocks:
paused_by:
done:      0.13.0 released. Bumped .claude-plugin/plugin.json, suite green at 527 tests, --validate passing. Verified live: --version reports 0.13.0 from /home/max/.claude-work/plugins/cache/moltke/moltke/0.13.0, at both user and project scope, and every live component there -- bin, hooks, skills, agents, templates, .claude-plugin -- matches the worktree. Not reached: /home/max/.claude, which installs from the git source and resolves against origin/master, so it stays on 0.12.0 at commit 6ca6455 until master is pushed; the agent never pushes (AGENTS.md 5). Max chose to close unreached over pushing first or repointing that root at the checkout (DEC-061), and MANUAL's Known issues names it as the state 0.13.0 shipped in. Established live, and written down: claude plugin update compares the manifest version alone, so against an unbumped checkout it prints 'already at the latest version' and copies nothing -- cache and lastUpdated untouched. A directory source therefore does not refresh without a bump, and the same comparison is why the CLI root's cache sat two commits behind its own recorded source at the same version. Scope is per install, not per root: the first update moved user scope only, leaving a live 0.12.0 at project scope in the same root, brought along with --scope project. README checked and corrected, MANUAL checked and corrected: both claimed an edit reaches the hooks after an update, without the bump that makes the update do anything. .moltke.local.md corrected the same way. MANUAL's Known issues now also names the two fence-dating gaps 0.13.0 ships with, open as S158.
author:    Maksym Bodnar
