id:         S139
goal:       install and verify 0.12.0, the merged plugin
accepts:    `--version` in a live session reports 0.12.0 from the plugin cache; the Monitor matcher refuses a hand-composed persistent watcher and allows the primitive, observed live rather than from a stdin fixture; SessionStart reports watch state; whatever the live session contradicts is recorded
touches:    nothing in the repository; this is a verification step
excludes:   changing behaviour to make the live session agree — that becomes its own step
decisions:  DEC-052
closes:
blocks:
paused_by:
done:      2026-08-19: verified live in a desktop session, and the live session contradicted the plan. --version from the answering install reports 'moltke 0.12.0 at /home/max/.claude-work/plugins/cache/moltke/moltke/0.12.0'. The Monitor arm lint refused both of its branches live rather than from a stdin fixture: 'timeout 30 tail -f LOG | grep -m1 RUN-DONE' blocked on the single-match follow branch, a persistent hand-composed 'until grep' loop blocked on the not-the-primitive branch, and the primitive itself armed and ran. A marker predating the arm was caught on the first scan, exit 0, which is the race a follow loses by construction. SessionStart injects: this session's own hook carried the stack, and a hand run while a watcher was alive carried 'watching: RUN-(DONE|FAILED) ... ceiling 2m'. Both watchers exited 0 on the success path, wrote their outcome under .git/moltke_watch/, and their records were deleted after acknowledgement (AGENTS.md 12). Contradicted and recorded as DEC-057: the plugin was absent from the desktop config root entirely, so no hooks, no skills, and no reviewer agent ran there — the Parked claim that 0.12.0 'is what the live hooks run' was false for every desktop session, and DEC-056's postponement of S138 is discharged, its reading of a loading question right and its mechanism narrower than assumed. MANUAL's 'Once per machine' is corrected to per config root, with the snapshot-not-reference consequence of a directory source; README gained the same for the dev loop. Suite 452 OK (3 skip), --validate green. specs.md unchanged: no behaviour moved. Left unstepped for the next plan, since this step excludes behaviour changes: nothing in a session reports which install is answering, so a stale or absent root still fails silently.
author:    Maksym Bodnar
