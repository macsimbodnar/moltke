id:         S118
goal:       bump 0.10.0 so the audit fixes ship
accepts:    acceptance criteria, testable
touches:    areas of the codebase
excludes:   explicitly out of scope
decisions:
closes:
blocks:
paused_by:
done:      2026-08-11: bumped 0.10.0 so the six audit fixes S112-S117 ship — the linked-worktree exclusion and the case-insensitive fence are the two that matter in live hooks. Suite 453 OK, --validate green. README and MANUAL checked, no change.
