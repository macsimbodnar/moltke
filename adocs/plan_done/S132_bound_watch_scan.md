id:         S132
goal:       bound each --watch scan so the ceiling holds against a runaway regex or a huge log
accepts:    a watcher armed with a catastrophically backtracking caller regex against a non-matching log still exits 124 at its ceiling, observed red first (today it runs past the ceiling until an outer kill); the bound is out of band from the poll loop, so a scan that never returns cannot outlive the deadline; a scan bounded mid-flight is reported as the ceiling, never as a silent no-match; the record under `.git/moltke_watch/` gets the same outcome it would on any other ceiling exit; the per-poll cost of a large log is stated in MANUAL rather than left to be discovered
touches:    bin/moltke.py _watch_scan and mode_watch, tests/test_s129_watch.py, MANUAL.md
excludes:   rejecting caller regexes by shape (guessing which patterns backtrack is not the fix); incremental or offset-based scanning, which would give up catching a marker written before arming
decisions:  DEC-049
closes:     2026-08-18_adversarial-F02
blocks:
paused_by:
done:      The ceiling bounded the poll loop, not the process: _watch_scan read the whole log and ran a caller-supplied regex with nothing consulting the clock, so (a+)+$ over a non-matching line hung inside search and a 1s ceiling ran until an outer kill. The bound is now out of band — an ITIMER_REAL alarm armed for whatever is left of the ceiling, covering the read and the search both — and a scan cut mid-flight raises _ScanTimeout, which exits 124 through the same path as a deadline arriving between polls, so the .git/moltke_watch/ record reads outcome ceiling exactly like any other ceiling exit and never a silent no-match falling through to the dead-pid check. Red was three new tests each hitting subprocess.TimeoutExpired at 15s against --ceiling 1s; that red run leaked three spinning watchers into the machine, so run_watch now kills a child that outlives its timeout. Windows has no setitimer, so the arm says so on stderr and the ceiling still holds between polls, observed with a driver that patches HAS_SCAN_ALARM off. MANUAL now states the per-poll cost of a large log and that the ceiling bounds the scan; specs.md's --watch row states the timer. README checked: it carries no --watch surface, unchanged. F02 stays planned until S138's re-run, per the close-on-re-run convention.
author:    Maksym Bodnar
