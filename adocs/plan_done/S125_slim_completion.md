id:         S125
goal:       the completion ceremony is slimmed (DEC-048)
accepts:    acceptance criteria, testable
touches:    areas of the codebase
excludes:   explicitly out of scope
decisions:
closes:
blocks:
paused_by:
done:      2026-08-11: the completion ceremony is slimmed (DEC-048). The stamp stays required and becomes free text — multi-line accepted, written as indented continuations, folding back whitespace-normalized through parse_step_file. The README/MANUAL substring gates and the testing-row requirement are gone from --step done, the Stop arrival gate, and INV-5, which now judges stamp presence alone; --goal keeps its one-line refusal because a plan entry is one line. The habit of checking the docs stays in the skill as a habit, not a police. Suite 395 OK, --validate green. README, MANUAL, specs, and the step skill all state the slimmed model.
author:    Maksym Bodnar
