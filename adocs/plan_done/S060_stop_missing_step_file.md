id:         S060
goal:       --stop enforces instead of crashing when a plan_done/ arrival is not on disk
accepts:    the stamp gate skips a porcelain entry that is not an existing file before parse_step_file reads it, so the AD, RD, and collapsed `?? adocs/plan_done/` shapes each exit 2 or 0 and never 1; mode_stop and mode_post_write gain the OSError refusal mode_step got in S052, so an unreadable tree names the path instead of raising; mode_stop reads porcelain with -uall like worktree_state, or handles the collapsed directory entry explicitly, so both gates and the audit baseline read one shape; the problems collected before the crash are printed in every case, verified with a fixture holding an INV-3 violation, a stale status.md, and the stamp complaint together; red observed with the traceback and the empty stderr on all three shapes
touches:    bin/moltke.py mode_stop stamp gate, mode_post_write; tests/test_s005_hooks.py
excludes:   changing which statuses count as an arrival, which S050 settled; the pre-write fence on plan_done/, which is what makes the mv remedy the only way out
decisions:
closes:     2026-08-08_adversarial-F01
blocks:
paused_by:
done:      2026-08-08: no mode ends in a traceback. Every invariant runs through one run_checks, which turns an OSError into a violation naming the check, and main carries a backstop for everything else a broken tree reaches, exiting 2 or 0 for --log-prompt and --session-start, which must never block. Three call sites ran the invariant loop and none handled a read failure. The stamp gate skips an entry that is not a file on disk, which is the AD and RD shapes, and RD is what following the gate's own remedy produces: it blocks, --pre-write refuses to edit the file it names, so mv back is the only way out. --stop reads porcelain with -uall like worktree_state, so the collapsed  entry is gone; that also made the gate see arrivals before the first commit, where it now abstains as the recap gate already did. 6 tests, red observed on all shapes, plus the finding's remedy path re-measured: exit 2 with the problems printed where it measured exit 1 and silence. Suite 291 OK, --validate green. README test count 286 to 291; MANUAL's stamp entry and exit-code section both updated; specs gained a dated note.
