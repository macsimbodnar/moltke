id:         S130
goal:       arm-time watcher allowlist and watch-state reporting in hooks
accepts:    `--pre-command` blocks a Monitor arm that pipes `tail -f`/`-F` into grep (exit 2, message carrying the primitive invocation), blocks a persistent Monitor arm that does not invoke `moltke.py --watch` unless the command carries the explicit MOLTKE_UNBOUNDED_OK token, allows the primitive, allows non-persistent arms, and exits 0 for other tools — each observed red-first from stdin JSON fixtures; Monitor's real tool_input field names are verified against the live tool schema before wiring and recorded; hooks.json adds the PreToolUse Monitor matcher; `--session-start` and `--step status` surface watch records (live, crashed, unacknowledged outcome); `--stop` blocks while an unacknowledged outcome record exists, naming the file to delete; specs gains INV-17, the CLI row, and dated notes; AGENTS.md §12 gains the one enforcement sentence, template identical; MANUAL mode row and hook list updated, README test count, golden refreshed; plugin.json 0.4.0
touches:    bin/moltke.py, hooks/hooks.json, tests/test_s015_precommand.py, tests/test_s005_hooks.py, tests/golden/cli_surface.txt, AGENTS.md, templates/AGENTS.md, adocs/specs.md, MANUAL.md, README.md, .claude-plugin/plugin.json
excludes:   linting Bash tool commands (foreground is turn-bounded, background is harness-tracked; DEC-049); enforcement outside Claude Code (standing non-goal); scanning process trees for leaked watchers
decisions:  DEC-049
closes:
blocks:
paused_by:
done:      2026-08-18: --pre-command shipped behind the PreToolUse Monitor matcher, tool_input shape verified against the live Monitor schema before wiring; DEC-051 narrows the lint (single-match follows always blocked, persistent non-primitive arms blocked without MOLTKE_UNBOUNDED_OK, bounded streams pass); watch records reach --session-start and --step status, and --stop blocks on crashed or unacknowledged watches. 15 tests red-first, suite 126 -> 141 green, --validate clean. README checked: test count updated. MANUAL checked: daily-use hook list, mode row, persistent-arm and token guidance. AGENTS.md section 13 enforcement paragraph, template identical. plugin.json 0.4.0.
