id:         S059
goal:       verify the installed release in a live session, after reinstall
accepts:    the installed plugin resolves to the version this repository ships — 0.6.0 after S066, since ten more steps landed while 0.5.0 was never installed — rather than the cached older one, evidenced from ~/.claude/plugins/installed_plugins.json and the cached bin/moltke.py; all five hook events observed firing in a live session against this repository, with the SessionStart context, a logged prompt, a refused write into plan_done/, a post-write scan, and a Stop refusal each recorded verbatim; the reviewer fence observed against a live plugin subagent spawn rather than a synthetic payload, which is what S012's clause 6 and finding F02 left unproven; status.md's parked note about the installed version being stale is removed in the same commit, or narrowed to what is still true
touches:    adocs/audit/ or adocs/testing.md for the observations; adocs/status.md; MANUAL.md if any documented behaviour differs live
excludes:   the reinstall itself, which is Max's own (claude plugin install moltke@moltke); pinning any particular version number here, since the point is that what runs matches what is committed; changing behaviour to fix what the live session reveals, which becomes its own step
decisions:  DEC-034
closes:     2026-08-06_adversarial-F02
blocks:
paused_by:
done:       2026-08-09: the installed plugin is 0.6.0 at gitCommitSha c2e6ad3, this checkout's HEAD, and the cached bin/moltke.py is byte-identical to the working tree, so what hooks execute is what is committed and the parked note claiming neither release was ever installed is false and is removed. All five hook events observed live and recorded verbatim in adocs/audit/2026-08-09_verification.md: the SessionStart context, a logged prompt, a refused write into plan_done/ that landed nothing, PostToolUse and Stop passing silently on a clean tree, and a Stop refusal at the natural gap between the plan_done/ move and the status.md regeneration. F02's open clause is closed by a live moltke:adversarial_reviewer spawn — blocked outside the fence, permitted under adocs/audit/, both sides seen, so the block is non-vacuous. Two things this records rather than hides: the PostToolUse refusal path is not live-observed, because producing one means introducing a real violation, which this step excludes, and it stays suite-covered; and the first Stop refusal exposed a defect this step's excludes send to their own step, S095 — a multi-line done: stamp is silently truncated to its first line by parse_step_file, so the gate rejected a stamp that did record the check. Suite 376 OK, --validate green. README and MANUAL checked, no change needed: the step verified documented behaviour rather than altering any, and MANUAL's fence description matches the message observed live.
