id:         S123
goal:       AGENTS.md reissued once - team rules in, trimmed to the operative core
accepts:    acceptance criteria, testable
touches:    areas of the codebase
excludes:   explicitly out of scope
decisions:
closes:
blocks:
paused_by:
done:      2026-08-11: AGENTS.md reissued once for the whole batch — team rules in (claim-on-start, per-author plan_active_max, merge semantics pointing at MANUAL's Teams section), the worklog section gone, review as the three tiers, decisions and testing rules matching DEC-046/048, trimmed 18,009 to 9,798 bytes so the one always-loaded file costs half. Section numbering compacted; the single code reference to a section (§1 in derived_next's docstring) still points at the reading protocol. Template byte-identical, generic test caught a DEC reference in the first draft. Suite 419 OK, --validate green. README and MANUAL checked — both were corrected in S120-S122 and needed nothing further.
author:    Maksym Bodnar
