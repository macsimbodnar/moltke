id:         S104
goal:       bump 0.8.0 and stop the audit loop by decision
accepts:    The full suite and every check are green at the bumped commit: 444
            tests, --validate exit 0, --audit list exit 0, --audit check exit 0.
            .claude-plugin/plugin.json reads 0.8.0 and the golden plugin test
            still passes, so the install path fetches the seven fixes committed
            since 0.7.0.
            A decisions.md entry records that the audit loop stops here by Max's
            decision rather than by DEC-035's severity rule, with what that costs.
            The seven 2026-08-09_adversarial findings stay `planned`, not
            `closed`: their fixes landed with red-first tests, and §10 closes a
            finding only on a re-run that no longer reports it. Claiming closed
            without that re-run would be the exact substitution — "I fixed it"
            for "the audit no longer finds it" — the rule exists to prevent.
            status.md says the project is stable and awaiting orders, in the
            Parked block, so a resumed session reads it there rather than
            inferring it from an empty plan.
touches:    .claude-plugin/plugin.json; adocs/decisions.md; adocs/status.md;
            adocs/testing.md
excludes:   re-running the audit, which is what this step decides not to do;
            marking any finding closed; reinstalling the plugin, which is Max's
            own
decisions:  DEC-035, DEC-041
closes:
blocks:
paused_by:
done:      2026-08-09: bumped .claude-plugin/plugin.json to 0.8.0 at a green tree and stopped the audit loop by decision (DEC-041) rather than by DEC-035's severity rule. Checks at the bump: 444 tests OK, --validate exit 0, --audit list exit 0, --audit check exit 0, the entries it lists being this session's own fix commits against the 2026-08-09 baseline. The bump is what lets the install path fetch the seven fixes committed since 0.7.0. The seven 2026-08-09_adversarial findings deliberately stay planned rather than closed: each has a completed step and red-first tests, and what is missing is the independent re-measurement a re-run would give, so calling them closed would substitute I fixed it for the audit no longer finds it, which is the one substitution the rule exists to prevent. status.md's Parked block now says the project is complete and stable at 0.8.0 and awaiting orders, so an empty plan reads as the finished state rather than as a gap. README and MANUAL checked, no change: no test count moved and no documented behaviour changed.
