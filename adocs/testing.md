# Testing ledger

Acceptance criteria with their covering tests. Rows are added with the feature,
never after. Append only.

| Step | Criterion | Covering test | Result |
|---|---|---|---|
| S001 | `project/` matches the AGENTS.md §2 file map; `CLAUDE.md` contains `@AGENTS.md` | manual shell check (S001, pre-suite; automated by `--validate` from S002) | pass 2026-08-01 |
| S001 | `.workflow.json` parses; `schema` 1, `enabled` true, `surface_guard` `cli` | manual `json.load` check (S001) | pass 2026-08-01 |
| S001 | step ids unique across plan dirs; every `plan_todo/` and `plan_current/` file listed in `plan.md` | manual check (S001; automated by INV-3/INV-6 tests in S003) | pass 2026-08-01 |
| S001 | `decisions.md` holds exactly DEC-012..DEC-001, unique, newest first | manual check (S001; automated by INV-9 test in S004) | pass 2026-08-01 |
| S001 | `decisions.md` ordered oldest first, DEC-001..newest at bottom, ids unique (DEC-013; supersedes the newest-first row above) | manual check (automated by INV-9 test in S004) | pass 2026-08-01 |
| S001 | `.moltke.json` parses; `schema` 1, `enabled` true, `surface_guard` `cli` (DEC-015; supersedes the `.workflow.json` row above) | manual `json.load` check | pass 2026-08-01 |
| S002 | INV-11: every mode exits 0 in an unmarked repo | tests/test_s002_marker.py TestInv11MarkerGate.test_every_mode_exits_0_without_marker | pass 2026-08-01 |
| S002 | INV-11: every mode exits 0 when `enabled` is false, even alongside other marker errors | TestInv11MarkerGate.test_every_mode_exits_0_when_disabled, test_disabled_beats_other_marker_errors | pass 2026-08-01 |
| S002 | marker is found from any subdirectory of the repo | TestInv11MarkerGate.test_marker_found_from_subdirectory | pass 2026-08-01 |
| S002 | `--validate` exits 1 and names every marker violation: unreadable JSON, non-object, schema, enabled, limits, surface_guard; all reported, not just the first | tests/test_s002_marker.py TestValidateMarker (red observed against the S002 stub: 9 failures, `0 != 1`; output in worklog recap) | pass 2026-08-01 |
| S002 | `--validate` exits 0 with "all checks pass" on a valid fixture and on this repository | TestValidateMarker.test_valid_marker_passes + `python3 bin/moltke.py --validate` here | pass 2026-08-01 |
| S003 | INV-1: more than `plan_active_max` non-paused steps in `plan_current/` is a violation; paused steps do not count | test_s003_invariants.py test_inv1_too_many_non_paused_steps, test_inv1_paused_steps_do_not_count | pass 2026-08-01 |
| S003 | INV-2: stack depth over `plan_stack_max` is a violation | test_inv2_stack_depth_exceeded | pass 2026-08-01 |
| S003 | INV-3: a step file in `plan_todo/` or `plan_current/` absent from `plan.md` is a violation; missing `plan.md` in an enabled repo too | test_inv3_step_missing_from_plan | pass 2026-08-01 |
| S003 | INV-4: a `plan_done/` step still named in another step's `blocks:` is a violation | test_inv4_done_step_still_blocking | pass 2026-08-01 |
| S003 | INV-5: a done step without a `done:` stamp or without a `testing.md` row is a violation | test_inv5_done_step_without_stamp, test_inv5_done_step_without_testing_row | pass 2026-08-01 |
| S003 | INV-6: duplicate step ids across the three plan directories are a violation | test_inv6_duplicate_step_id | pass 2026-08-01 |
| S003 | INV-7: modifying or deleting a tracked `plan_done/` file is a violation; adding one is legal; no git baseline means abstain | test_inv7_modified_done_step, test_inv7_deleted_done_step, test_inv7_added_done_step_is_allowed | pass 2026-08-01 |
| S003 | valid tree passes: the base fixture satisfies INV-1..INV-7 (non-vacuity anchor for every broken variant) | test_valid_tree_passes + `python3 bin/moltke.py --validate` here | pass 2026-08-01 |
| S004 | INV-8: rewriting earlier bytes of `decisions.md` or `worklog.md`, or deleting either, is a violation; appending is legal; no git baseline means abstain | test_s004_invariants.py TestAppendOnly (4 tests) | pass 2026-08-01 |
| S004 | INV-9: duplicate `DEC-<nnn>` ids in decisions.md are a violation | TestDecisionIds.test_duplicate_dec_id_fails | pass 2026-08-01 |
| S004 | INV-10: a finding status outside open/planned/closed/accepted is a violation; an open finding with no step `closes:` reference and no decisions.md mention is a violation; referenced or accepted findings pass | TestAuditFindings (4 tests) | pass 2026-08-01 |
| S005 | `--log-prompt` appends the verbatim prompt with a timestamp and never exits non-zero, even on damaged stdin (exit 2 would erase the prompt) | test_s005_hooks.py TestLogPrompt (2 tests) | pass 2026-08-01 |
| S005 | `--pre-write` exits 2 for writes under `plan_done/` and for step files outside the three plan directories; ordinary writes pass; path arrives as argument or hook stdin JSON | TestPreWrite (4 tests) | pass 2026-08-01 |
| S005 | `--session-start` reports the `plan_current/` stack and the derived next step as `additionalContext` JSON, and flags a stale `status.md` | TestSessionStart (2 tests) | pass 2026-08-01 |
| S005 | `--post-write` surfaces cheap-scan violations on stderr with non-blocking semantics; clean repos stay silent | TestPostWrite (2 tests) | pass 2026-08-01 |
| S005 | `--stop` exits 2 on invariant violations, stale status.md, and source changes without a worklog recap; a recap unblocks; after 3 consecutive blocks it allows the stop (no-deadlock cap) | TestStop (6 tests) | pass 2026-08-01 |
| S006 | `--scaffold` creates the marker, AGENTS.md, CLAUDE.md, the Cursor pointer, and a populated `project/`; the scaffolded repo passes `--validate` | test_s006_scaffold.py TestScaffold.test_creates_the_full_tree, test_scaffolded_repo_validates_clean | pass 2026-08-01 |
| S006 | `--scaffold` is idempotent and never overwrites an existing file, including a foreign `AGENTS.md`; user edits survive a second run | TestScaffold.test_running_twice_is_idempotent, test_existing_agents_md_is_never_overwritten | pass 2026-08-01 |
| S006 | `--scaffold` leaves a declined repo untouched and scaffolds at the git root when run from a subdirectory | TestScaffold.test_declined_repo_is_left_untouched, test_scaffolds_at_git_root_from_a_subdirectory | pass 2026-08-01 |
| S006 | a freshly scaffolded repo reports no phantom next step, no stale status.md, and does not block the first stop (regression: commented-out example steps in plan.md are not the plan) | TestScaffold.test_fresh_scaffold_is_immediately_clean (red observed: `'stale' unexpectedly found in ... derived next step: s001`) | pass 2026-08-01 |
| S006 | `--decline` writes a durable disabled marker and refuses to disable an already-enabled repository | test_s006_scaffold.py TestDecline (2 tests) | pass 2026-08-01 |
| S006 | templates carry no live decision-id cross-references in prose (DEC-002), and `templates/AGENTS.md` is byte-identical to `AGENTS.md` (DEC-012) | TestTemplatesAreGeneric (2 tests; red observed on the shipped ruleset's DEC-013 reference and the template's comment-wrapped example) | pass 2026-08-01 |
| S006 | the `init` skill exists and declares name and description frontmatter | TestInitSkill.test_skill_declares_name_and_description | pass 2026-08-01 |
| S007 | `--step new` allocates the next free id, writes the step file, lists it in plan.md; ids are never reused even after a file is deleted | test_s007_step.py TestNew (2 tests) | pass 2026-08-01 |
| S007 | `--step start` promotes todo to current and is refused when the active slot is taken or the step is unknown, naming the holder; a refusal leaves the tree clean | TestStart (3 tests) | pass 2026-08-01 |
| S007 | `--step block` creates the child with `blocks:`, pauses the parent, and is refused past `plan_stack_max` with the replan instruction, or when the parent is not active | TestBlock (3 tests) | pass 2026-08-01 |
| S007 | `--step done` is refused, naming the condition, when the step is not current, is paused, is still named in an open step's `blocks:`, has no testing.md row, or the stamp omits the README and MANUAL check | TestDone.test_refuses_* (5 tests) | pass 2026-08-01 |
| S007 | `--step done` stamps and moves the step, unpauses its parent, and lets the parent complete afterwards; `--validate` clean throughout | TestDone.test_completes_and_stamps, test_completing_a_child_unpauses_the_parent, test_parent_completes_once_its_child_is_done | pass 2026-08-01 |
| S007 | `--step status` regenerates from the filesystem, shows the paused stack, and preserves the human-written Parked list | TestStatus (3 tests) | pass 2026-08-01 |
| S007 | an unfilled `paused_by` template placeholder does not hide an active step (INV-1 still fires) | TestTemplatePlaceholders (red observed: `0 != 1 : moltke: all checks pass`) | pass 2026-08-01 |
| S007 | the `step` skill exists and declares name and description frontmatter | TestStepSkill.test_skill_declares_name_and_description | pass 2026-08-01 |
| S008 | `--audit new <type>` creates the dated report and refuses to overwrite an existing one; a fresh report has no live findings and leaves `--validate` clean | test_s008_audit.py TestAuditNew (3 tests) | pass 2026-08-01 |
| S008 | `--audit list` prints each finding with status and what references it, and exits non-zero while an open finding has neither step nor decision | TestAuditList (2 tests) | pass 2026-08-01 |
| S008 | INV-10 requires a finding id to carry its own report's name | TestFindingIds.test_finding_id_must_match_its_report | pass 2026-08-01 |
| S008 | the reviewer may write only under `project/audit/`: source, specs, plan, and step files are blocked; other agents are unaffected | TestReviewerWriteFence (4 tests) | pass 2026-08-01 |
| S008 | `adversarial_reviewer` is defined with Read and Write but never Edit; the `audit` skill declares name and description | TestDefinitions (2 tests) | pass 2026-08-01 |
| S008 | a scaffolded repo's first real `DEC-001` does not collide with the template's format example (regression: template guidance is not data) | test_s006_scaffold.py test_first_real_decision_does_not_collide_with_the_template_example (red observed before `strip_guidance`) | pass 2026-08-01 |
| S009 | the golden fails when a flag or `--step`/`--audit` operation is added, renamed, or removed | test_s009_surface.py TestGoldenSurface (red observed twice by tampering: added `--purge` flag, added `abandon` operation) | pass 2026-08-01 |
| S009 | refreshing the golden alone does not make the suite green: every flag and operation must appear in the specs CLI table, an operation only where its own mode is described | TestSurfaceIsDocumented.test_specs_describes_every_mode (red observed: `Lists differ: ['--purge'] != []`, `['--step abandon'] != []`) | pass 2026-08-01 |
| S009 | the same check runs against MANUAL.md once it exists | TestSurfaceIsDocumented.test_manual_describes_every_mode_once_it_exists | skipped 2026-08-01, activates in S011 |
| S009 | the DEC-012 ruleset identity guard is present and not duplicated | TestRulesetIdentity.test_identity_guard_is_present (comparison itself lives in test_s006_scaffold.py) | pass 2026-08-01 |
| S010 | `plugin.json` carries a kebab-case name matching the documented `/moltke:<skill>` namespace, an explicit semantic version (DEC-002), a description, and no path field pointing at a missing component | test_s010_plugin.py TestManifest (4 tests) | pass 2026-08-01 |
| S010 | every skill directory names itself in frontmatter so it resolves as `/moltke:<skill>`; skills are not hidden inside `.claude-plugin/`; the reviewer agent is discoverable; hooks call the checker through `${CLAUDE_PLUGIN_ROOT}` and the checker is executable | TestComponentsAreDiscoverable (4 tests) | pass 2026-08-01 |
| S010 | the marketplace entry resolves to this plugin and does not contradict the manifest version | TestMarketplace (2 tests) | pass 2026-08-01 |
| S010 | `claude plugin validate --strict` passes on the marketplace manifest, and on the plugin manifest, skills, agents, and hooks | TestClaudeValidates (skips without the claude CLI; run here: marketplace "Validation passed", plugin copy passed apart from the DEC-020 root-CLAUDE.md warning) | pass 2026-08-01 |
| S011 | MANUAL.md describes every flag and every `--step`/`--audit` operation, each operation on a line naming its own mode | test_s009_surface.py TestSurfaceIsDocumented.test_manual_describes_every_mode_once_it_exists (no longer skipped; red observed by deleting two modes: `Lists differ: ['--decline', '--audit list'] != []`) | pass 2026-08-02 |
| S011 | README states the exact build, test, validate, and golden-refresh commands, and that no environment variables are read | manual verification against the code: `grep environ bin/moltke.py` returns nothing; every command in README run here | pass 2026-08-02 |
| S011 | MANUAL records the known issues as claims about code: plugin ships repo state, mechanical docs gate, reviewer fence depends on `agent_type`, INV-7/8 abstain without git, live hooks unverified | manual verification against DEC-020, `--step done` stamp check, `mode_pre_write`, `inv_7`/`inv_8` abstain branches, and S012 being open | pass 2026-08-02 |
| S013 | `--scaffold` creates `adocs/` and never `project/` (DEC-021) | test_s013_adocs.py TestScaffoldNamesAdocs.test_scaffold_creates_adocs_and_never_project (red observed by pointing SCAFFOLD_DIRS back at `project/`: `AssertionError: True is not false : scaffold created project/`) | pass 2026-08-02 |
| S013 | no live documentation file still names `project/`; history is exempt | test_s013_adocs.py TestNoStaleProjectPaths.test_live_docs_name_adocs (red observed by inserting one stale line into README: `Lists differ: ['README.md:2: stale ref to project/status.md'] != []`) | pass 2026-08-02 |
| S013 | every path and user-facing message in `bin/moltke.py` reads `adocs/`, through the single `DOCS` constant | `grep -n project bin/moltke.py` returns only the DEC-021 comment on line 17; full suite green on the renamed fixtures | pass 2026-08-02 |
| S013 | `AGENTS.md` and `templates/AGENTS.md` stay byte-identical across the rename | test_s006_scaffold.py test_template_agents_matches_repo_agents + `cmp AGENTS.md templates/AGENTS.md` | pass 2026-08-02 |
| S013 | `worklog.md` and `decisions.md` survive the move byte-identical (INV-8 has no HEAD baseline at the new path for one commit and abstains) | md5sum before and after the move, both `fc2750fa…` and `2ed9d790…`; abstain branch confirmed by `git show HEAD:adocs/worklog.md` returning 128 in a throwaway clone | pass 2026-08-02 |
| S013 | INV-7 reads the moved `plan_done/` files as additions, the one legal change | `git status --porcelain -- adocs/plan_done` in a throwaway clone reports `A` for every file, not `D` or `R` | pass 2026-08-02 |
| S012 | `claude plugin marketplace add` and `claude plugin install moltke@moltke` succeed, and `/moltke:init`, `/moltke:step`, `/moltke:audit` resolve | manual, two machines (Max, 2026-08-06); `claude plugin list` reports `moltke@moltke`; all three skills listed by the session | pass 2026-08-06 |
| S012 | all five hook events fire in a live session against the `adocs/` build | manual live probes 2026-08-06, one per event: SessionStart printed the stack and `Derived next step: S012`; UserPromptSubmit appended the prompt verbatim; PreToolUse refused a write into `plan_done/` and a step file at the repo root; PostToolUse surfaced `INV-3` on an unlisted step file without blocking the write; Stop refused a stale `status.md` naming the fix | pass 2026-08-06 |
| S012 | the `adversarial_reviewer` subagent is spawnable through the installed plugin | manual live spawn 2026-08-06 of `moltke:adversarial_reviewer`, which ran and reported back | pass 2026-08-06 |
| S012 | the reviewer write fence holds under a real subagent spawn | manual live probe 2026-08-06: the spawned reviewer wrote `PROBE_S012.md` to the repo root and reported `File created successfully`, with no hook denial; the path matches no step-file pattern and is not under `plan_done/`, so the fence was the only rule that could have blocked it | **fail 2026-08-06**; recorded as finding F02 in `adocs/audit/2026-08-06_adversarial.md`, fixed by S016 which re-probes with a live spawn |
| S012 | DEC-002 confirmed before the first push | `git remote -v` reports `origin git@github.com:macsimbodnar/moltke.git`; `origin/master` at `1064774` matched local HEAD before this session's commits | pass 2026-08-06 |
| S014 | `--log-prompt` in a marked repo whose `adocs/` is absent still records the prompt | test_s005_hooks.py TestLogPrompt.test_prompt_survives_a_missing_docs_directory (red observed before the `mkdir`: `AssertionError: False is not true : prompt lost: /var/folders/.../adocs/worklog.md was never created`) | pass 2026-08-06 |
| S014 | a swallowed worklog append is reported through `SessionStart` `additionalContext`, the only channel a zero exit reaches | test_s005_hooks.py TestLogPromptFailureIsLoud.test_failed_append_is_reported_at_session_start (red observed: `AssertionError: 'not appended' not found in 'moltke stack (plan_current/):\n  S003: active\nDerived next step: S002 ...'`); non-vacuous by construction — the test asserts the append really failed before asserting the report | pass 2026-08-06 |
| S014 | the report fires once and stops when the failure stops, so a fixed path needs no manual clearing | test_s005_hooks.py TestLogPromptFailureIsLoud.test_report_stops_once_the_failure_stops (red observed: same `'not appended' not found` on the first of the two calls) | pass 2026-08-06 |
| S014 | a healthy append reports nothing, so the failure line is not permanent noise | test_s005_hooks.py TestLogPromptFailureIsLoud.test_a_healthy_append_reports_nothing (control; green before and after the fix by design) | pass 2026-08-06 |
| S015 | with a prompt logged first, as every live session does, `--stop` still blocks a source change that has no recap | test_s005_hooks.py TestStop.test_blocks_source_change_without_recap, and every TestStop fixture now calls `log_prompt` before stopping (red observed: `AssertionError: 0 != 2 : ` with empty stderr — the gate was entirely silent) | pass 2026-08-06 |
| S015 | a recap heading after the last logged prompt unblocks | test_s005_hooks.py TestStop.test_recap_unblocks_source_change | pass 2026-08-06 |
| S015 | a recap older than the last logged prompt does not discharge the later turn | test_s005_hooks.py TestStop.test_recap_older_than_the_last_prompt_does_not_count (red observed: `AssertionError: 0 != 2`) | pass 2026-08-06 |
| S015 | a recap heading inside a fenced block is guidance, not a recap | test_s005_hooks.py TestStop.test_recap_inside_a_fenced_block_does_not_count (red observed: `AssertionError: 0 != 2`) | pass 2026-08-06 |
| S015 | a heading that reads as both, `recap … prompt`, counts as a recap — this repo's own S014 recap heading has that shape | test_s005_hooks.py TestStop.test_recap_about_prompts_is_still_a_recap (red observed against the ambiguous last-prompt-wins classification: `AssertionError: 2 != 0 : moltke: source changed but adocs/worklog.md has no recap heading after the last logged prompt…`) | pass 2026-08-06 |
| S015 | the recap gate abstains before the first commit, so a fresh scaffold does not block | test_s005_hooks.py TestStop.test_recap_gate_abstains_before_the_first_commit; non-vacuous — it asserts the same tree blocks once a commit exists (red observed while the abstain was missing: test_s006_scaffold.py `AssertionError: 2 != 0 : a freshly scaffolded repo must not block the first stop`) | pass 2026-08-06 |
| S016 | the PreToolUse `agent_type` a plugin-spawned reviewer actually sends is observed, not assumed | manual live probe 2026-08-06: the installed 0.2.0 hook was instrumented to dump its payload, then each agent was spawned through the plugin. Reviewer: `'moltke:adversarial_reviewer'` with `agent_id` present. Built-in subagent: `'general-purpose'`. Main thread: both keys absent. Installed file restored byte-identical, md5 `c52956752a8b022715f7dfdf49ca52d9` before and after | pass 2026-08-06 |
| S016 | a reviewer write outside `adocs/audit/` is blocked for the scoped and the bare `agent_type` alike | test_s008_audit.py TestReviewerWriteFence.test_reviewer_cannot_write_source and test_reviewer_cannot_write_plan_or_specs (red observed with the observed value: `AssertionError: 0 != 2 : ('moltke:adversarial_reviewer', '')`, empty stderr — the fence emitted nothing) | pass 2026-08-06 |
| S016 | the reviewer may still write its own report | test_s008_audit.py TestReviewerWriteFence.test_reviewer_may_write_its_report; also observed live — the reviewer's in-fence write to `adocs/audit/.probe_s016_agent_type.md` succeeded | pass 2026-08-06 |
| S016 | unrelated subagents are unaffected, including a similarly named agent from another plugin | test_s008_audit.py TestReviewerWriteFence.test_other_agents_are_unaffected over `general-purpose`, `Explore`, `other:adversarial-reviewer` | pass 2026-08-06 |
| S016 | the main thread, which sends no `agent_type`, is never fenced | test_s008_audit.py TestReviewerWriteFence.test_main_thread_is_unaffected | pass 2026-08-06 |
| S016 | a live reviewer spawn asked to write outside the fence declined on its own instructions, so it is not evidence about the hook | manual live spawn 2026-08-06; recorded because a self-refusal and a working fence are indistinguishable from outside, which is why the payload was captured directly instead | **not evidence 2026-08-06**; the fence claim rests on the payload observation and the suite rows above |
| S017 | `--audit new` records a working-tree baseline and `--audit check` reports this run's report as expected | test_s008_audit.py TestAuditReconciliation.test_only_the_reports_own_changes_are_expected (red observed: `AssertionError: 1 != 0 : moltke: unknown --audit operation 'check'; use one of new, list`) | pass 2026-08-06 |
| S017 | a new file under `tests/` is expected; a modified existing test and any source change are unexpected and exit 1 | test_s008_audit.py TestAuditReconciliation.test_a_new_test_file_is_expected, test_a_modified_existing_test_is_unexpected, test_a_source_change_is_unexpected | pass 2026-08-06 |
| S017 | the check is correct from a dirty starting tree: pre-existing changes are in the baseline and never attributed to the run | test_s008_audit.py TestAuditReconciliation.test_a_dirty_starting_tree_is_not_blamed_on_the_audit | pass 2026-08-06 |
| S017 | a second edit to a file that was already dirty is caught, though its porcelain status never moves | test_s008_audit.py TestAuditReconciliation.test_a_second_change_to_an_already_dirty_file_is_caught; this is what the per-file sha256 in the baseline buys | pass 2026-08-06 |
| S017 | reverting a change that was in the tree before the audit is unexpected, not invisible | test_s008_audit.py TestAuditReconciliation.test_reverting_someone_elses_change_is_unexpected | pass 2026-08-06 |
| S017 | `--audit check` with no baseline refuses and names `--audit new` rather than passing quietly | test_s008_audit.py TestAuditReconciliation.test_check_without_a_baseline_refuses_and_says_what_to_run | pass 2026-08-06 |
| S017 | the report is not hidden inside an untracked `adocs/audit/` directory | red observed during the build: with plain `git status --porcelain` the check reported `adocs/audit/: ??` as unexpected and exited 1; fixed by `-uall`, covered by test_only_the_reports_own_changes_are_expected | pass 2026-08-06 |
| S017 | the fence permits a new file under `tests/`, blocks editing an existing test, and still blocks everywhere else | test_s008_audit.py TestReviewerMayWriteNewTests (red observed: `AssertionError: 2 != 0 : moltke: the adversarial_reviewer may only write under adocs/audit/, and tests/test_regression.py is outside it`) | pass 2026-08-06 |
| S017 | no live document still claims `Bash` is fenced or that the hook is the only real limit | specs.md S008 sentence marked superseded in the same commit; `agents/adversarial_reviewer.md`, `skills/audit/SKILL.md`, `MANUAL.md`, `README.md` rewritten; golden refreshed with `check` documented in the specs table and MANUAL, which test_s009_surface.py enforces | pass 2026-08-06 |
| S030 | rewriting or deleting `adocs/worklog.md` is no longer an INV-8 violation, while the identical tampering against `decisions.md` still is | test_s004_invariants.py TestAppendOnly.test_rewriting_or_deleting_the_worklog_is_no_longer_a_violation, re-targeted from test_rewriting_worklog_fails rather than deleted; non-vacuous by construction — it asserts the decisions.md violation first (red observed: `AssertionError: 1 != 0 : VIOLATION: INV-8: adocs/worklog.md changed earlier bytes; append-only files grow only at the end`) | pass 2026-08-06 |
| S030 | `decisions.md` rewriting, deletion, and legal appends behave exactly as before | test_s004_invariants.py TestAppendOnly.test_rewriting_decisions_fails, test_deleting_append_only_file_fails, test_appending_is_legal, all unchanged | pass 2026-08-06 |
| S030 | `AGENTS.md` and `templates/AGENTS.md` stay byte-identical, and the shipped ruleset cites no live decision id | test_s006_scaffold.py test_ruleset_template_matches_the_live_ruleset and test_no_live_decision_ids_in_template_prose (red observed: `AssertionError: Lists differ: ['DEC-025', 'DEC-025'] != [] : AGENTS.md references live decision ids`, caught the first draft citing DEC-025 in prose that ships to other repositories) | pass 2026-08-06 |
| S018 | modifying a `plan_done/` file is still a violation after the tampering is committed | test_s003_invariants.py TestInvariants.test_inv7_survives_the_commit_that_hides_it; asserts the uncommitted violation first, so a green result cannot come from the check having stopped working (red observed: `AssertionError: 0 != 1 : moltke: all checks pass` after the commit) | pass 2026-08-06 |
| S018 | deleting a `plan_done/` file is still a violation after the deletion is committed | test_s003_invariants.py TestInvariants.test_inv7_survives_a_committed_deletion (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S018 | rewriting `decisions.md` is still a violation after the rewrite is committed | test_s004_invariants.py TestAppendOnly.test_committing_a_rewrite_does_not_hide_it, precondition asserted uncommitted first (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S018 | deleting `decisions.md` is still a violation after the deletion is committed | test_s004_invariants.py TestAppendOnly.test_committed_deletion_of_decisions_is_caught (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S018 | committed appends to `decisions.md` stay legal across several commits | test_s004_invariants.py TestAppendOnly.test_committed_appends_stay_legal | pass 2026-08-06 |
| S018 | additions to `plan_done/` stay legal, and repositories with no history still abstain | test_s003_invariants.py TestInvariants.test_inv7_added_done_step_is_allowed and test_inv7_abstains_without_history; the abstain test is non-vacuous — the same tampering with history present is reported by the tests above | pass 2026-08-06 |
| S018 | this repository's own history is clean under the new checks, including the S013 `project/` to `adocs/` rename | `git log --reverse --no-renames --name-status -- adocs/plan_done` reports `A` for all 18 entries and nothing else; `git log --numstat -- adocs/decisions.md` shows no commit with deletions; `--validate` exit 0 | pass 2026-08-06 |
| S019 | a finding id inside a fenced block in `decisions.md` no longer discharges an open finding | test_s008_audit.py TestGuidanceNeverDischargesAFinding.test_a_fenced_example_does_not_discharge_a_finding; the baseline violation is asserted first, as the audit reproduced it, so exit 1 is not merely the check being broken (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S019 | the same holds for an HTML comment | test_s008_audit.py TestGuidanceNeverDischargesAFinding.test_an_html_comment_does_not_discharge_a_finding (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S019 | a real `decisions.md` entry naming the finding still discharges it | test_s008_audit.py TestGuidanceNeverDischargesAFinding.test_a_real_entry_still_discharges_it | pass 2026-08-06 |
| S019 | `--audit list` agrees with `--validate` and no longer prints "referenced in decisions.md" for a fenced mention | test_s008_audit.py TestGuidanceNeverDischargesAFinding.test_audit_list_agrees_with_validate (red observed: `AssertionError: 0 != 1 : 2026-08-01_adversarial-F01  open  (referenced in decisions.md)`) | pass 2026-08-06 |
| S019 | the tightening does not orphan any finding in this repository | `--audit list` after the change still attributes all 14 findings of `2026-08-06_adversarial.md` to steps S014..S026, exit 0; none was relying on a fenced reference | pass 2026-08-06 |
| S020 | a second `--audit new` of the same type on the same date creates a suffixed report and leaves the first untouched | test_s008_audit.py TestAuditNew.test_a_same_day_rerun_suffixes_instead_of_overwriting, re-targeted from test_refuses_to_overwrite_an_existing_report and still asserting the no-overwrite property (red observed: `AssertionError: 1 != 0 : moltke: adocs/audit/2026-08-06_adversarial.md already exists ... re-run the audit tomorrow.`) | pass 2026-08-06 |
| S020 | further re-runs keep counting up, `.2` then `.3` | test_s008_audit.py TestAuditNew.test_reruns_keep_counting_up (red observed: same already-exists refusal) | pass 2026-08-06 |
| S020 | a re-run report names its own finding-id stem, so its first finding does not violate INV-10 | test_s008_audit.py TestAuditNew.test_a_rerun_report_states_its_own_finding_ids (red observed: `AssertionError: '2026-08-06_adversarial.2-F01' not found in ''`) | pass 2026-08-06 |
| S020 | a re-run finding id does not satisfy the first report, which the suffix would have allowed under `startswith` | test_s008_audit.py TestAuditNew.test_a_rerun_finding_id_does_not_satisfy_the_first_report; INV-10's stem check is now an exact `<stem>-F<nn>` match | pass 2026-08-06 |
| S021 | with `test_command` set, `--step done` runs it and refuses on a non-zero exit, naming the command and showing the output tail | test_s007_step.py TestDoneTestCommandGate.test_a_failing_command_refuses_and_shows_the_output; also asserts the step stays in `plan_current/`, so a refusal never half-completes (red observed: `AssertionError: 0 != 1 : moltke: S003 completed and moved to plan_done/`) | pass 2026-08-06 |
| S021 | a passing command lets the step complete | test_s007_step.py TestDoneTestCommandGate.test_a_passing_command_lets_the_step_complete | pass 2026-08-06 |
| S021 | with the key absent, behaviour is exactly as before and `--step done` says nothing ran the suite | test_s007_step.py TestDoneTestCommandGate.test_without_the_key_nothing_runs_and_the_gap_is_stated (red observed: `AssertionError: 'test_command' not found in 'moltke: S003 completed and moved to plan_done/...'`) | pass 2026-08-06 |
| S021 | a non-string `test_command` is a marker violation | test_s007_step.py TestDoneTestCommandGate.test_a_non_string_test_command_is_a_marker_violation (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S021 | a blank `test_command` is a marker violation, not a silent no-op | test_s007_step.py TestDoneTestCommandGate.test_a_blank_test_command_is_a_marker_violation | pass 2026-08-06 |
| S021 | the gate runs from the repository root, not the caller's directory | test_s007_step.py TestDoneTestCommandGate.test_the_gate_runs_from_the_repository_root, invoked from `adocs/plan_todo/` with a command that checks for `.moltke.json` | pass 2026-08-06 |
| S021 | schema stays 1, so no existing marker migrates | the key is validated only when present; every other test in the suite uses a marker without it and still passes, 151 tests | pass 2026-08-06 |
| S021 | this repository's own gate is live | `.moltke.json` sets `test_command` to `python3 -m unittest discover -s tests`; S021's own `--step done` ran it and printed `moltke: running the suite gate` before completing | pass 2026-08-06 |
| S022 | the suite fails when a prefixed key shape or a PEM private-key header appears in `adocs/worklog.md` | test_s022_secrets.py TestWorklog.test_the_worklog_holds_no_secret_shapes; red observed by planting a synthetic AWS key in the real worklog and restoring it: `AssertionError: Lists differ: [('AWS access key id', 709, 'AKIAIOSF')] != []`, exit 1 | pass 2026-08-06 |
| S022 | the detector is proven to work before it is pointed at the real file | test_s022_secrets.py TestDetector.test_every_shape_catches_its_known_bad_example and test_the_scan_reports_a_planted_secret, one subtest per shape; this guard caught a real defect on first run — the Google API key example was 36 characters against a 35-character pattern: `AssertionError: None is not true : Google API key no longer matches its own example; the check below would pass by failing to look` | pass 2026-08-06 |
| S022 | git shas, md5 digests, UUIDs, and ordinary prose never trip it | test_s022_secrets.py TestDetector.test_benign_strings_never_fire over real shas from this session, `c52956752a8b022715f7dfdf49ca52d9`, a UUID, `npm install`, and a PEM certificate header | pass 2026-08-06 |
| S022 | the failure names the shape and line without printing the whole value | the assertion message reports label, line number, and the first 8 characters only, verified by the planted-secret red above reporting `'AKIAIOSF'` | pass 2026-08-06 |
| S023 | adding a skill fails the golden and the documentation cross-check | red observed by creating `skills/bogus/SKILL.md` and running the suite: `AssertionError: ... 'skills: audit,bogus,init,step\n' != ... 'skills: audit,init,step\n'` plus `AssertionError: Lists differ: ['skill bogus'] != []` from MANUAL; directory removed afterwards | pass 2026-08-06 |
| S023 | removing a hook event fails the golden | baseline observed first, before the fix: deleting the `Stop` event from `hooks/hooks.json` left the whole suite green, `suite exit with Stop hook deleted: 0 / OK`. After the fix the same tampering gives `AssertionError: ... 'hooks: PostToolUse,PreToolUse,SessionStart,UserPromptSubmit' != ... ',SessionStart,Stop,UserPromptSubmit'`; file restored afterwards | pass 2026-08-06 |
| S023 | adding a recognised marker key fails the golden | red observed by appending `lint_command` to `MARKER_KEYS`: `AssertionError: ... 'enabled,lint_command,plan_active_max,...' != 'enabled,plan_active_max,...'`; `bin/moltke.py` restored afterwards | pass 2026-08-06 |
| S023 | `MARKER_KEYS` cannot become a decorative list | test_s009_surface.py TestMarkerKeysAreLoadBearing.test_every_declared_key_is_actually_validated, one subtest per key; the same tampering above also tripped it: `AssertionError: False is not true : lint_command is in MARKER_KEYS but check_marker ignores it, so the golden guards a key that means nothing` | pass 2026-08-06 |
| S023 | an unrecognised marker key stays ignored, so no existing marker breaks | test_s009_surface.py TestMarkerKeysAreLoadBearing.test_an_unrecognised_key_is_ignored_not_rejected, with test_the_good_marker_is_accepted as the non-vacuity anchor | pass 2026-08-06 |
| S023 | the skill set and hook event set are discovered, not hardcoded | test_s010_plugin.py test_each_skill_directory_names_itself now iterates `declared_skills()`, and test_every_declared_hook_event_has_a_command asserts every declared event carries a command rather than asserting only that some command exists | pass 2026-08-06 |
| S023 | the golden still declares all three component kinds, so the checks cannot pass vacuously | test_s009_surface.py TestGoldenSurface.test_the_golden_covers_more_than_argparse | pass 2026-08-06 |
| S024 | an id listed in `plan.md` with no step file in any of the three directories is an INV-3 violation naming the id | test_s003_invariants.py TestInvariants.test_inv3_plan_id_with_no_step_file (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-06 |
| S024 | the existing forward direction still holds | test_s003_invariants.py TestInvariants.test_inv3_step_missing_from_plan, unchanged | pass 2026-08-06 |
| S024 | a commented example step in `plan.md` is still guidance, not a phantom id | test_s003_invariants.py TestInvariants.test_inv3_ignores_a_phantom_id_that_is_only_guidance; without this the scaffolded plan template would make every fresh repository violate INV-3 on creation | pass 2026-08-06 |
| S024 | an id whose step file sits in `plan_done/` is present, not phantom | test_s003_invariants.py TestInvariants.test_inv3_counts_a_completed_step_as_present, the non-vacuity anchor for the row above | pass 2026-08-06 |
| S024 | a phantom id is no longer silent | manual probe 2026-08-06 in a throwaway repo whose `plan.md` lists `S099` first: `--validate` exit 1, `--post-write` exit 2, and `--stop` exit 2 all print `INV-3: plan.md lists S099 but no step file exists ...`. Residual, deliberately out of scope: `--session-start` still reports `Derived next step: S099`, because `derived_next` is unchanged by this step's excludes | pass 2026-08-06 |
| S025 | findings exit 1 on stdout: `--validate`, `--audit list`, `--audit check` | test_s025_exit_codes.py TestFindingsGoToStdout, which also asserts stderr stays empty | pass 2026-08-06 |
| S025 | refusals exit 1 on stderr: `--step` transitions, an unknown operation, `--audit check` with no baseline | test_s025_exit_codes.py TestRefusalsGoToStderr; red observed by routing `refuse` to stdout, which is what README used to claim: three failures, `AssertionError: '' is not true : a refusal must say why, on stderr`; `bin/moltke.py` restored and verified | pass 2026-08-06 |
| S025 | blocks exit 2 on stderr, and `--post-write` returns 2 while being non-blocking by contract | test_s025_exit_codes.py TestBlocksGoToStderr | pass 2026-08-06 |
| S025 | clean paths exit 0 and stay quiet, so the assertions above are not passing on a tool that always fails | test_s025_exit_codes.py TestCleanPathsAreQuietAndZero | pass 2026-08-06 |
| S025 | stderr on an exit 0 path is documented, because it breaks the "stderr means failure" assumption | test_s025_exit_codes.py TestCleanPathsAreQuietAndZero.test_stderr_can_carry_a_warning_on_a_zero_exit, over `--audit new` outside a git worktree | pass 2026-08-06 |
| S025 | the documented mapping was derived by observation, not from reading the code alone | manual probe 2026-08-06 across eleven invocations in two throwaway repos, one without git and one with, recording exit code and which streams carried output for each; the README and MANUAL tables were written from that matrix | pass 2026-08-06 |
| S026 | `plugin.json` carries a `repository` field, verified as a real manifest key before adding it | live plugin reference fetched 2026-08-06, field table row: `repository` / string / "Source code URL" / `"https://github.com/user/plugin"`; `claude plugin validate .` reports `Validation passed` with the field present | pass 2026-08-06 |
| S026 | the documented install URL resolves publicly | `https://github.com/macsimbodnar/moltke` fetched unauthenticated and returned the repository page, so MANUAL's hosted install command is checked rather than assumed | pass 2026-08-06 |
| S026 | all four plan directories survive being empty, as `--scaffold` already guarantees for a scaffolded repo | `.gitkeep` added to `adocs/plan_todo/`, `plan_current/`, and `plan_done/`; `adocs/audit/.gitkeep` was already tracked. `SCAFFOLD_DIRS` names all four, so moltke now matches what it scaffolds | pass 2026-08-06 |
| S026 | README's Python claim matches what is actually run | drift found by sweeping, beyond the three items in the finding: README said 3.12 while the suite runs on the system 3.9.6. Both `python3` (3.9.6) and `/opt/homebrew/bin/python3.14` (3.14.6) run 173 tests OK; README now states exactly that and claims no lower bound | pass 2026-08-06 |
| S026 | compiled bytecode is no longer tracked | `git log --oneline -1 -- tests/__pycache__` showed `1064774 Rename the workflow directory project/ to adocs/ (S013)`, so twelve Python 3.12 `.pyc` files were committed and, under DEC-020, copied into every plugin install. Removed and `__pycache__/` added to `.gitignore`; suite green afterwards | pass 2026-08-06 |
| S026 | README's "no environment variables" claim holds | `grep -n "os.environ\|getenv\|environ" bin/moltke.py` returns nothing | pass 2026-08-06 |
| S026 | README's "five hook events" and "three skills" claims hold | `hooks/hooks.json` declares 5 events and `skills/*/SKILL.md` declares 3, both now pinned by the S023 golden rather than only by prose | pass 2026-08-06 |
| S027 | `plugin.json` is 0.3.0 and the manifest still validates | `claude plugin validate .` reports `Validation passed` after the bump | pass 2026-08-07 |
| S027 | the audit was re-run and every 2026-08-06 finding got a verdict decided against the code, not against the step files | `adocs/audit/2026-08-07_adversarial.md`, section "Verdicts on the 2026-08-06 findings": 12 no longer reproduce, F03 still reproduces by decision, F02 is unsettleable in this checkout. Each verdict carries its own evidence — file:line or a command and its output | pass 2026-08-07 |
| S027 | twelve findings closed, one accepted, one held with the reason stated | `--audit list` shows F01, F04..F14 `closed`, F03 `accepted` under DEC-022, F02 `planned` because the installed plugin is still pinned to 0.2.0 at `1064774` whose bare equality is the defect | pass 2026-08-07 |
| S027 | the re-run reconciled cleanly | `--audit check` exit 1 with exactly one unexpected entry, `adocs/worklog.md`, which is the plugin's own `UserPromptSubmit` appending prompts during the run — the false positive the re-run itself reported as 2026-08-07_adversarial-F05, reproduced live here. `git diff` confirmed the change is two prompt headings and nothing else | pass 2026-08-07 |
| S027 | eleven new findings each end in exactly one plan step | `--audit list` exit 0 with S032..S042 named against F01..F11 one-to-one; no bundling | pass 2026-08-07 |
| S027 | the new tests written since the last audit are not vacuous | the reviewer mutated twelve of the new behaviours in a copy of the repository and recorded twelve reds, no survivors; a negative result, recorded because "the suite is green" and "the suite would notice" are different claims | pass 2026-08-07 |
| S027 | plan.md prose can silently reorder the plan, found while regenerating status at the end of this step | after listing the new steps, `--step status` wrote `Next: S028` while the ordered list began at S034, because a description paragraph read "ahead of the feature work S028, S029, and S031" and `derived_next` takes the first id in document order. `--validate` stayed green throughout: INV-3's reverse direction passes, since every prose id has a step file. Prose ids removed, `Next: S034` restored, and the weakness planned as S045 | **fail 2026-08-07**; planned as S045, which also names the three pre-existing prose ids in this file as its fixtures |
| S045 | an id named in prose above the list does not become the derived next step | test_s007_step.py TestPlanOrderComesFromTheList.test_prose_above_the_list_does_not_become_the_next_step (red observed: `AssertionError: 'S002' != 'S003'`) | pass 2026-08-07 |
| S045 | an id inside a list entry's own text does not reorder it, which `plan.md`'s S010 entry does with S012 | test_s007_step.py TestPlanOrderComesFromTheList.test_an_id_inside_a_list_entrys_own_text_does_not_reorder_it | pass 2026-08-07 |
| S045 | list order still decides, so the two above are not passing by always returning the last id | test_s007_step.py TestPlanOrderComesFromTheList.test_list_order_still_decides | pass 2026-08-07 |
| S045 | a commented-out list entry is still not the plan | test_s007_step.py TestPlanOrderComesFromTheList.test_a_commented_list_entry_is_still_not_the_plan | pass 2026-08-07 |
| S045 | the fix holds on the real plan, not only on fixtures | manual probe 2026-08-07: inserted `The closure run is S044, and the feature work is S028.` above the description of the real `adocs/plan.md`; `--session-start` still reported `Derived next step: S045`. File restored and verified byte-identical | pass 2026-08-07 |
| S045 | INV-3 still reads the whole file, so a prose id with no step file is still reported | unchanged `inv_3_steps_in_plan`, covered by test_s003_invariants.py test_inv3_plan_id_with_no_step_file | pass 2026-08-07 |
| S034 | a repair commit clears a committed `plan_done/` tampering, and the tampering is still caught before it | test_s003_invariants.py test_inv7_a_repair_commit_clears_it, which asserts the violation first (red observed: after following the prescribed fix, `AssertionError: 1 != 0 : VIOLATION: INV-7: adocs/plan_done/S001_base.md was M in commit db91aca8 ...`) | pass 2026-08-07 |
| S034 | only the original bytes clear it, so the row above is not passing on any repair-shaped commit | test_s003_invariants.py test_inv7_a_near_miss_repair_does_not_clear_it | pass 2026-08-07 |
| S034 | a committed deletion clears when the file is restored | test_s003_invariants.py test_inv7_a_committed_deletion_clears_when_the_file_comes_back | pass 2026-08-07 |
| S034 | a repair commit clears a committed `decisions.md` rewrite | test_s004_invariants.py test_a_repair_commit_clears_a_committed_rewrite (red observed: `AssertionError: 1 != 0 : VIOLATION: INV-8: adocs/decisions.md lost 1 line(s) in commit ffa40654 ...`) | pass 2026-08-07 |
| S034 | putting the removed text back at the end is not putting it back | test_s004_invariants.py test_a_repair_that_appends_instead_of_restoring_does_not_clear_it | pass 2026-08-07 |
| S034 | committed rewrites and deletions of `decisions.md` are still caught, and committed appends stay legal | test_s004_invariants.py test_committing_a_rewrite_does_not_hide_it, test_committed_deletion_of_decisions_is_caught, test_committed_appends_stay_legal, all unchanged from S018 | pass 2026-08-07 |
| S034 | the mechanism was chosen by measurement, not by argument | two candidate rules were implemented and run against the suite before the third was kept: forgiving a version when a longer one is a prefix cleared a genuine same-position rewrite, and pairwise adjacent checks did not clear a correct repair. Recorded as DEC-027 with both results | pass 2026-08-07 |
| S034 | the gap the chosen rule leaves is measured, not assumed | throwaway repo probe 2026-08-07: legitimate append exit 0; post-baseline rewrite uncommitted exit 1; the same rewrite committed exit 0; baseline rewrite committed exit 1 | **known gap 2026-08-07**; planned as S046, stated in specs and MANUAL |
| S046 | a committed rewrite of text appended after the first commit is caught | test_s004_invariants.py test_a_committed_rewrite_of_post_baseline_text_is_caught, which first asserts the legitimate append is clean (red observed: `AssertionError: 0 != 1 : moltke: all checks pass`) | pass 2026-08-07 |
| S046 | repairing that rewrite clears it, so both halves hold at once | test_s004_invariants.py test_a_repair_of_post_baseline_text_clears_it (red observed twice: first as `0 != 1` under the DEC-027 baseline rule, then as `1 != 0 : VIOLATION: INV-8: ... no longer contains, in order, the lines it had at commit 10cbde36` under the every-version subsequence rule, which is why the mark skips tampered versions) | pass 2026-08-07 |
| S046 | the five states DEC-027 measured were re-measured against the new rule | throwaway repo probe 2026-08-07: legitimate append exit 0; post-baseline rewrite uncommitted exit 1; the same rewrite committed exit 1, where DEC-027 measured 0; the repair exit 0; baseline rewrite committed exit 1 | pass 2026-08-07 |
| S046 | every earlier immutability behaviour still holds | test_s004_invariants.py TestAppendOnly in full, unchanged from S018 and S034: committed rewrite caught, committed deletion caught, committed appends legal, worklog exempt, append-at-end is not a repair | pass 2026-08-07 |
| S035 | the `Stop` deadlock cap fires in a linked worktree exactly as in a clone | test_s005_hooks.py TestStop.test_block_cap_prevents_deadlock_in_a_linked_worktree, which asserts `.git` really is a file first so the test cannot pass on a plain clone (red observed: the five exit codes read `[2, 2, 2, 2, 2]` against the required `[2, 2, 2, 0, 0]`) | pass 2026-08-07 |
| S035 | a linked worktree gets an audit baseline, and `--audit check` reconciles there | test_s008_audit.py TestAuditReconciliationInAWorktree.test_a_baseline_is_recorded_and_check_reconciles (red observed: `AssertionError: 'no git' unexpectedly found in 'moltke: no git worktree here, so --audit check cannot reconcile this run...'`) | pass 2026-08-07 |
| S035 | a directory with no git still abstains, and the warning now says what it means | test_s008_audit.py TestAuditReconciliationInAWorktree.test_without_git_the_warning_is_accurate_and_check_still_refuses; non-vacuity for the row above, since the fix must not make every directory look like a repository | pass 2026-08-07 |
| S035 | the fix holds for the submodule case the finding also named, not only the worktree | manual probe 2026-08-07 across three real repositories: clone `[2,2,2,0,0]`, linked worktree `[2,2,2,0,0]` where it had been `[2,2,2,2,2]`, submodule `[2,2,2,0,0]`; `.git` confirmed a file in the latter two. `--audit new` in the worktree exits 0 with empty stderr and `--audit check` exits 0 | pass 2026-08-07 |
| S033 | a trailing unclosed fence no longer swallows the rest of the file, and no longer shifts the pairing of later fences | test_s033_fences.py TestUnbalancedFencesKeepData (red observed: `AssertionError: '### KEEP' not found in '\n\n### ALSO KEEP\n'`) | pass 2026-08-07 |
| S033 | balanced fences and HTML comments are still stripped, so the original job is intact | test_s033_fences.py TestBalancedFencesStillStrip, the non-vacuity anchor | pass 2026-08-07 |
| S033 | only a marker that opens a line is a fence, so a marker quoted in a worklog prompt or written inline is not | test_s033_fences.py TestOnlyLineStartMarkersAreFences (red observed: the quoted-prompt case lost its recap heading) | pass 2026-08-07 |
| S033 | two unclosed fences are reported, not guessed | test_s033_fences.py TestAmbiguityIsReportedNotGuessed over the worklog and an audit report; the strip-level test asserting this case was re-targeted rather than deleted once the templates proved no heuristic can resolve it | pass 2026-08-07 |
| S033 | no content heuristic could have resolved the ambiguity | measured 2026-08-07 across every template: `templates/audit_report_template.md`, `templates/adocs/decisions.md`, and `AGENTS.md` each place a markdown heading inside a fence deliberately, which is exactly what `strip_guidance` exists to hide | pass 2026-08-07 |
| S033 | the finding's second live reproduction, the silently disabled recap gate, is fixed | throwaway repo probe 2026-08-07: a recap body opening a fence it never closes, followed by a prompt pasting a fenced snippet, now gives `--stop` exit 2 where the finding measured exit 0, and `--validate` exit 1 naming `INV-13: adocs/worklog.md has 1 code-fence markers` | pass 2026-08-07 |
| S033 | this repository's own scanned files are balanced, so INV-13 was not added on top of an existing violation | scan of `plan.md`, `decisions.md`, `worklog.md`, `specs.md`, `testing.md`, every audit report and every step file: no odd counts | pass 2026-08-07 |
| S032 | a clean tracked file the run patches and commits is reported as unexpected | test_s008_audit.py TestAuditReconciliation.test_a_committed_source_patch_is_unexpected (red observed: `AssertionError: 0 != 1 : no change since --audit new; the report itself has not been written yet.` — the misleading message the finding quoted) | pass 2026-08-07 |
| S032 | the same holds for the case the fence exists to stop, a weakened existing test, committed | test_s008_audit.py TestAuditReconciliation.test_a_committed_weakened_test_is_unexpected | pass 2026-08-07 |
| S032 | committing the report and a new regression test stays expected | test_s008_audit.py TestAuditReconciliation.test_committing_the_report_and_a_new_test_stays_expected; non-vacuity, since the fix must not call every commit contamination | pass 2026-08-07 |
| S032 | a commit made before `--audit new` is not blamed on the run | test_s008_audit.py TestAuditReconciliation.test_a_commit_made_before_the_run_is_not_blamed_on_it | pass 2026-08-07 |
| S032 | the finding's own F1 scenario was re-run against the fix | throwaway repo probe 2026-08-07: reviewer patches `src/main.py` and commits, `--audit check` exits 1 reporting `src/main.py: M in a commit made during this run` as unexpected while the report's own commit stays expected; the finding measured exit 0 | pass 2026-08-07 |
| S032 | a baseline `HEAD` that history has made unreachable is reported, not skipped | `audit_check` reports it when `git diff` against the recorded sha fails, so a rewritten history during a run is loud rather than silently unverifiable | pass 2026-08-07 |
| S038 | a malformed `test_command` refuses completion and leaves the step in `plan_current/` | test_s007_step.py TestDoneTestCommandGate.test_a_malformed_test_command_refuses_completion, one subtest for each of the four values the finding measured (red observed: `AssertionError: 0 != 1 : moltke: no "test_command" in .moltke.json ... moltke: S003 completed and moved to plan_done/`) | pass 2026-08-07 |
| S038 | the refusal no longer claims the key is absent when it is present | asserted inside the same test; the wrong message was half the finding | pass 2026-08-07 |
| S038 | a marker violation refuses every `--step` operation, not only `done` | test_s007_step.py TestDoneTestCommandGate.test_a_malformed_marker_refuses_every_step_transition over `start`, `new`, and `status` | pass 2026-08-07 |
| S038 | an absent key behaves exactly as before | test_s007_step.py TestDoneTestCommandGate.test_without_the_key_nothing_runs_and_the_gap_is_stated, unchanged from S021 | pass 2026-08-07 |
| S038 | the finding's four values were re-measured against the fix | throwaway repo probe 2026-08-07 over `''`, `'   '`, a list, and `0`: all four give exit 1 with `moved_to_done=False`, where the finding measured exit 0 and a completed step in every case | pass 2026-08-07 |
| S039 | a wrong `In progress` or `Last done` is reported by `--session-start`, not only a wrong `Next` | test_s005_hooks.py TestStatusStaleness.test_a_wrong_in_progress_is_reported and test_a_wrong_last_done_is_reported (red observed: the context read only the stack and the derived next step, with no staleness line) | pass 2026-08-07 |
| S039 | `--stop` refuses a stale in-progress stack | test_s005_hooks.py TestStatusStaleness.test_stop_refuses_a_stale_in_progress (red observed: `AssertionError: 0 != 2`) | pass 2026-08-07 |
| S039 | regenerating clears it, and an accurate `status.md` is never reported | test_s005_hooks.py TestStatusStaleness.test_regenerating_clears_it and test_an_accurate_status_is_not_reported, the non-vacuity anchor | pass 2026-08-07 |
| S039 | the `Updated:` line and the Parked block stay the human's to write | test_s005_hooks.py TestStatusStaleness.test_the_parked_block_and_the_date_are_the_humans_to_write, which edits both and expects silence | pass 2026-08-07 |
| S039 | the finding's own state is reported | throwaway repo probe 2026-08-07 with `- In progress: none` and a step in `plan_current/`: `--session-start` says `status.md is stale (In progress says 'none', filesystem says 'S003 active')` and `--stop` exits 2 naming the field, where the finding measured silence from both | pass 2026-08-07 |
| S039 | the stricter comparison did not paper over two things it exposed | a fresh scaffold read as stale because the template says `no steps planned yet` while the code said `no steps left in plan.md`, so `status_lines` now distinguishes the two states; and `Last done` was read from every id in `plan.md` rather than through `plan_order`, the same defect S045 fixed for `derived_next`. Both covered by test_s006_scaffold.py test_fresh_scaffold_is_immediately_clean and the suite at large | pass 2026-08-07 |
| S037 | a change to `.claude-plugin/plugin.json` requires a recap like any other source file | test_s005_hooks.py TestStop.test_which_paths_count_as_source, a table of nine representative paths with one subtest each (red observed: `AssertionError: 0 != 2 : .claude-plugin/plugin.json` and the same for `.clauderc` and `.claudefoo`) | pass 2026-08-07 |
| S037 | `.claude/settings.json` and `adocs/` stay exempt, so the fix did not simply make everything source | same table, the non-vacuity half: both are asserted to allow | pass 2026-08-07 |
| S037 | the exemption is by directory, not by stem, so a neighbouring path is not exempt by accident | same table covers `.clauderc`, `.claudefoo`, and `adocsfoo/notes.md`, all required to block | pass 2026-08-07 |
| S037 | the finding's measured table was re-measured against the fix | throwaway repo probe 2026-08-07 over the finding's six paths: `.claude-plugin/plugin.json` and `.claudefoo` moved from allow to block, `src/main.py` and `bin/moltke.py` still block, `.claude/settings.json` and `adocs/specs.md` still allow | pass 2026-08-07 |
| S036 | a worklog append during the run is expected, not blamed on the reviewer | test_s008_audit.py TestAuditReconciliation.test_a_worklog_append_during_the_run_is_expected (red observed: `AssertionError: 1 != 0 : ... unexpected, not attributable to writing the report: adocs/worklog.md: M`) | pass 2026-08-07 |
| S036 | the same holds once the turn's prompts are committed | test_s008_audit.py TestAuditReconciliation.test_a_committed_worklog_append_is_also_expected, since S032 made commits part of the footprint and the exemption would otherwise not survive a commit | pass 2026-08-07 |
| S036 | a worklog rewrite is still reported, so the exemption is scoped to appends | test_s008_audit.py TestAuditReconciliation.test_a_worklog_rewrite_during_the_run_is_still_unexpected and test_a_worklog_append_after_a_rewrite_is_still_unexpected | pass 2026-08-07 |
| S036 | the finding's isolation sequence was re-measured | throwaway repo probe 2026-08-07: immediately after `--audit new` exit 0; after one user prompt exit 0 where the finding measured exit 1; after the reviewer truncates the worklog exit 1 with it listed as unexpected | pass 2026-08-07 |
| S036 | a thin fixture can make the rewrite case look like an append, and the real tests do not use one | found while probing: with a one-line baseline worklog, replacing it with longer text that happens to start the same way is a genuine append and correctly passes. The unit tests use a worklog with prior content, which is what makes the truncation detectable | pass 2026-08-07 |
| S041 | a relative escape through an allowed directory is blocked for the reviewer | test_s008_audit.py TestReviewerMayWriteNewTests.test_a_relative_escape_through_tests_is_blocked over `tests/../bin/moltke.py`, `tests/../bin/newfile.py`, and `adocs/audit/../../bin/moltke.py` (red observed: `AssertionError: 0 != 2 : ('tests/../bin/moltke.py', '')`) | pass 2026-08-07 |
| S041 | the absolute form of the same path is blocked too, which is what the relative branch was brought into line with | test_s008_audit.py TestReviewerMayWriteNewTests.test_the_absolute_form_of_the_same_path_is_blocked_too, the non-vacuity anchor | pass 2026-08-07 |
| S041 | a genuine new test is still allowed, including a nested and a dot-prefixed path | test_s008_audit.py TestReviewerMayWriteNewTests.test_a_genuine_new_test_is_still_allowed | pass 2026-08-07 |
| S041 | the `plan_done` rule gets the same normalised path, since it reads the same `rel` | test_s005_hooks.py TestPreWrite.test_a_relative_escape_into_plan_done_is_blocked (red observed: `AssertionError: 0 != 2 : ('adocs/plan_todo/../plan_done/S001_base.md', '')`) | pass 2026-08-07 |
| S041 | a path resolving outside the repository stays unpoliced, deliberately | test_s005_hooks.py TestPreWrite.test_a_path_outside_the_repository_is_still_not_ours_to_police and test_s008_audit.py test_a_path_that_leaves_the_repository_stays_unpoliced; this narrows S041's own acceptance wording, which asked for any escape to be refused, to what the finding actually claimed | pass 2026-08-07 |
| S041 | the finding's measured table was re-measured | throwaway repo probe 2026-08-07 with a scoped reviewer payload: `bin/newfile.py` blocked, `tests/../bin/newfile.py` and `tests/../bin/moltke.py` now blocked where the finding measured allowed, the absolute equivalent still blocked, `tests/test_new.py` still allowed | pass 2026-08-07 |
| S040 | a type with a path separator is refused and nothing is created | test_s008_audit.py TestAuditNew.test_a_type_with_a_path_separator_is_refused, which also asserts `adocs/audit/` is left empty and no stray directory appears outside the repo (red observed: the report was created and `--audit new` exited 0) | pass 2026-08-07 |
| S040 | a dotted type is refused, because `.2` is the same-day re-run namespace S020 reserved | test_s008_audit.py TestAuditNew.test_a_dotted_type_is_refused_because_the_suffix_namespace_is_reserved | pass 2026-08-07 |
| S040 | spaces, tabs, dots, an empty type, and a shell metacharacter are all refused before reaching the filesystem | test_s008_audit.py TestAuditNew.test_awkward_types_are_refused_before_they_reach_the_filesystem, one subtest each (red observed: `AssertionError: 0 != 1 : moltke: created adocs/audit/2026-08-07_sec;rm.md`) | pass 2026-08-07 |
| S040 | the documented types still work, including hyphens and underscores | test_s008_audit.py TestAuditNew.test_ordinary_types_still_work over `adversarial`, `security`, `bugs`, `perf-2`, `dep_scan`; the non-vacuity anchor | pass 2026-08-07 |
| S040 | the finding's reproduction was re-run | throwaway repo probe 2026-08-07 with `../../outside/pwned`: exit 1 naming the rule, nothing created under `adocs/audit/`, and no stray directory outside the repository | pass 2026-08-07 |
| S042 | a failing `test_command` gate refuses entirely on stderr, writing nothing to stdout | test_s025_exit_codes.py TestRefusalsGoToStderr.test_a_failing_test_command_gate, the case that class stated a rule about and did not cover (red observed: `AssertionError: 'moltke: running the suite gate: ...' is not false : a refusal must not go to stdout`) | pass 2026-08-07 |
| S042 | the finding's split-stream measurement was re-run | throwaway repo probe 2026-08-07 with `test_command` set to `exit 3`: exit 1, `stdout: ''`, and the banner first on stderr, where the finding measured the banner on stdout and the refusal on stderr | pass 2026-08-07 |
| S042 | the unconfigured-gate notice stays on stdout, because it accompanies a completion rather than a refusal | test_s007_step.py TestDoneTestCommandGate.test_without_the_key_nothing_runs_and_the_gap_is_stated, unchanged from S021 and still asserting stdout | pass 2026-08-07 |
| S044 | `plugin.json` is 0.4.0 and the manifest still validates | `claude plugin validate .` reports `Validation passed` after the bump | pass 2026-08-07 |
| S044 | the re-run exercised S020's same-day suffix for real | `--audit new adversarial` on a date that already had a report created `adocs/audit/2026-08-07_adversarial.2.md`, with finding ids numbered from that stem, and did not overwrite the first | pass 2026-08-07 |
| S044 | every 2026-08-07 finding got a verdict decided by running the tool, not by reading the step files | `adocs/audit/2026-08-07_adversarial.2.md`, "Verdicts on the 2026-08-07 findings": ten no longer reproduce, F02 still reproduces on its primary path. Each verdict carries its own transcript | pass 2026-08-07 |
| S044 | ten closed, one held with the surviving case named | `--audit list`: F01 and F03..F11 `closed`, F02 `planned` because two unclosed fences are an even marker count and INV-13 only reports odd ones | pass 2026-08-07 |
| S044 | the reconciliation was clean, and S036's worklog exemption held on its first real run | `--audit check` exit 0 with only the report listed, on a run that spanned several prompts and so appended to `adocs/worklog.md`; before S036 that same append was reported as unexpected on every audit | pass 2026-08-07 |
| S044 | the installed plugin is still 0.2.0, proved rather than inferred | the `PostToolUse` hook reported 8 of the reviewer's 10 findings; running the cache copy and the checkout copy over the same input reproduces exactly that split, so the hook executes `~/.claude/plugins/cache/moltke/moltke/0.2.0/bin/moltke.py` | **not closed 2026-08-07**; 2026-08-06_adversarial-F02 stays planned until 0.4.0 is installed, which is Max's under DEC-014 |
| S044 | ten new findings each end in exactly one plan step | `--audit list` exit 0 with S047..S057 named against `.2-F01..F10` one-to-one, ordered by severity ahead of the feature work | pass 2026-08-07 |
| S044 | the suite would notice if the new behaviours were removed | the reviewer deleted 17 of them in copies outside the repository: 16 red, 1 survivor — the README/MANUAL stamp gate, which has no test and is recorded as 2026-08-07_adversarial.2-F03 | pass 2026-08-07 |
