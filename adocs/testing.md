# Testing ledger

Acceptance criteria with their covering tests. Rows are added with the feature,
never after. Append only.

| Step | Criterion | Covering test | Result |
|---|---|---|---|
| S001 | `project/` matches the AGENTS.md §2 file map; `CLAUDE.md` contains `@AGENTS.md` | manual shell check (S001, pre-suite; automated by `--validate` from S002) | pass 2026-08-01 |
| S001 | `.workflow.json` parses; `schema` 1, `enabled` true, `surface_guard` `cli` | manual `json.load` check (S001) | pass 2026-08-01 |
| S001 | step ids unique across plan dirs; every `plan_todo/` and `plan_current/` file listed in `plan.md` | manual check (S001; automated by INV-3/INV-6 tests in S003) | pass 2026-08-01 |
| S001 | `decisions.md` holds exactly DEC-012..DEC-001, unique, newest first | manual check (S001; automated by INV-9 test in S004) | pass 2026-08-01 |
| S001 | `decisions.md` ordered oldest first, DEC-001..newest at bottom, ids unique (DEC-013; supersedes the newest-first row above) | manual check (automated by INV-9 test in S004) | pass 2026-08-01 |
| S001 | `.moltke.json` parses; `schema` 1, `enabled` true, `surface_guard` `cli` (DEC-015; supersedes the `.workflow.json` row above) | manual `json.load` check | pass 2026-08-01 |
| S002 | INV-11: every mode exits 0 in an unmarked repo | tests/test_s002_marker.py TestInv11MarkerGate.test_every_mode_exits_0_without_marker | pass 2026-08-01 |
| S002 | INV-11: every mode exits 0 when `enabled` is false, even alongside other marker errors | TestInv11MarkerGate.test_every_mode_exits_0_when_disabled, test_disabled_beats_other_marker_errors | pass 2026-08-01 |
| S002 | marker is found from any subdirectory of the repo | TestInv11MarkerGate.test_marker_found_from_subdirectory | pass 2026-08-01 |
| S002 | `--validate` exits 1 and names every marker violation: unreadable JSON, non-object, schema, enabled, limits, surface_guard; all reported, not just the first | tests/test_s002_marker.py TestValidateMarker (red observed against the S002 stub: 9 failures, `0 != 1`; output in worklog recap) | pass 2026-08-01 |
| S002 | `--validate` exits 0 with "all checks pass" on a valid fixture and on this repository | TestValidateMarker.test_valid_marker_passes + `python3 bin/moltke.py --validate` here | pass 2026-08-01 |
| S003 | INV-1: more than `plan_active_max` non-paused steps in `plan_current/` is a violation; paused steps do not count | test_s003_invariants.py test_inv1_too_many_non_paused_steps, test_inv1_paused_steps_do_not_count | pass 2026-08-01 |
| S003 | INV-2: stack depth over `plan_stack_max` is a violation | test_inv2_stack_depth_exceeded | pass 2026-08-01 |
| S003 | INV-3: a step file in `plan_todo/` or `plan_current/` absent from `plan.md` is a violation; missing `plan.md` in an enabled repo too | test_inv3_step_missing_from_plan | pass 2026-08-01 |
| S003 | INV-4: a `plan_done/` step still named in another step's `blocks:` is a violation | test_inv4_done_step_still_blocking | pass 2026-08-01 |
| S003 | INV-5: a done step without a `done:` stamp or without a `testing.md` row is a violation | test_inv5_done_step_without_stamp, test_inv5_done_step_without_testing_row | pass 2026-08-01 |
| S003 | INV-6: duplicate step ids across the three plan directories are a violation | test_inv6_duplicate_step_id | pass 2026-08-01 |
| S003 | INV-7: modifying or deleting a tracked `plan_done/` file is a violation; adding one is legal; no git baseline means abstain | test_inv7_modified_done_step, test_inv7_deleted_done_step, test_inv7_added_done_step_is_allowed | pass 2026-08-01 |
| S003 | valid tree passes: the base fixture satisfies INV-1..INV-7 (non-vacuity anchor for every broken variant) | test_valid_tree_passes + `python3 bin/moltke.py --validate` here | pass 2026-08-01 |
| S004 | INV-8: rewriting earlier bytes of `decisions.md` or `worklog.md`, or deleting either, is a violation; appending is legal; no git baseline means abstain | test_s004_invariants.py TestAppendOnly (4 tests) | pass 2026-08-01 |
| S004 | INV-9: duplicate `DEC-<nnn>` ids in decisions.md are a violation | TestDecisionIds.test_duplicate_dec_id_fails | pass 2026-08-01 |
| S004 | INV-10: a finding status outside open/planned/closed/accepted is a violation; an open finding with no step `closes:` reference and no decisions.md mention is a violation; referenced or accepted findings pass | TestAuditFindings (4 tests) | pass 2026-08-01 |
| S005 | `--log-prompt` appends the verbatim prompt with a timestamp and never exits non-zero, even on damaged stdin (exit 2 would erase the prompt) | test_s005_hooks.py TestLogPrompt (2 tests) | pass 2026-08-01 |
| S005 | `--pre-write` exits 2 for writes under `plan_done/` and for step files outside the three plan directories; ordinary writes pass; path arrives as argument or hook stdin JSON | TestPreWrite (4 tests) | pass 2026-08-01 |
| S005 | `--session-start` reports the `plan_current/` stack and the derived next step as `additionalContext` JSON, and flags a stale `status.md` | TestSessionStart (2 tests) | pass 2026-08-01 |
| S005 | `--post-write` surfaces cheap-scan violations on stderr with non-blocking semantics; clean repos stay silent | TestPostWrite (2 tests) | pass 2026-08-01 |
| S005 | `--stop` exits 2 on invariant violations, stale status.md, and source changes without a worklog recap; a recap unblocks; after 3 consecutive blocks it allows the stop (no-deadlock cap) | TestStop (6 tests) | pass 2026-08-01 |
| S006 | `--scaffold` creates the marker, AGENTS.md, CLAUDE.md, the Cursor pointer, and a populated `project/`; the scaffolded repo passes `--validate` | test_s006_scaffold.py TestScaffold.test_creates_the_full_tree, test_scaffolded_repo_validates_clean | pass 2026-08-01 |
| S006 | `--scaffold` is idempotent and never overwrites an existing file, including a foreign `AGENTS.md`; user edits survive a second run | TestScaffold.test_running_twice_is_idempotent, test_existing_agents_md_is_never_overwritten | pass 2026-08-01 |
| S006 | `--scaffold` leaves a declined repo untouched and scaffolds at the git root when run from a subdirectory | TestScaffold.test_declined_repo_is_left_untouched, test_scaffolds_at_git_root_from_a_subdirectory | pass 2026-08-01 |
| S006 | a freshly scaffolded repo reports no phantom next step, no stale status.md, and does not block the first stop (regression: commented-out example steps in plan.md are not the plan) | TestScaffold.test_fresh_scaffold_is_immediately_clean (red observed: `'stale' unexpectedly found in ... derived next step: s001`) | pass 2026-08-01 |
| S006 | `--decline` writes a durable disabled marker and refuses to disable an already-enabled repository | test_s006_scaffold.py TestDecline (2 tests) | pass 2026-08-01 |
| S006 | templates carry no live decision-id cross-references in prose (DEC-002), and `templates/AGENTS.md` is byte-identical to `AGENTS.md` (DEC-012) | TestTemplatesAreGeneric (2 tests; red observed on the shipped ruleset's DEC-013 reference and the template's comment-wrapped example) | pass 2026-08-01 |
| S006 | the `init` skill exists and declares name and description frontmatter | TestInitSkill.test_skill_declares_name_and_description | pass 2026-08-01 |
| S007 | `--step new` allocates the next free id, writes the step file, lists it in plan.md; ids are never reused even after a file is deleted | test_s007_step.py TestNew (2 tests) | pass 2026-08-01 |
| S007 | `--step start` promotes todo to current and is refused when the active slot is taken or the step is unknown, naming the holder; a refusal leaves the tree clean | TestStart (3 tests) | pass 2026-08-01 |
| S007 | `--step block` creates the child with `blocks:`, pauses the parent, and is refused past `plan_stack_max` with the replan instruction, or when the parent is not active | TestBlock (3 tests) | pass 2026-08-01 |
| S007 | `--step done` is refused, naming the condition, when the step is not current, is paused, is still named in an open step's `blocks:`, has no testing.md row, or the stamp omits the README and MANUAL check | TestDone.test_refuses_* (5 tests) | pass 2026-08-01 |
| S007 | `--step done` stamps and moves the step, unpauses its parent, and lets the parent complete afterwards; `--validate` clean throughout | TestDone.test_completes_and_stamps, test_completing_a_child_unpauses_the_parent, test_parent_completes_once_its_child_is_done | pass 2026-08-01 |
| S007 | `--step status` regenerates from the filesystem, shows the paused stack, and preserves the human-written Parked list | TestStatus (3 tests) | pass 2026-08-01 |
| S007 | an unfilled `paused_by` template placeholder does not hide an active step (INV-1 still fires) | TestTemplatePlaceholders (red observed: `0 != 1 : moltke: all checks pass`) | pass 2026-08-01 |
| S007 | the `step` skill exists and declares name and description frontmatter | TestStepSkill.test_skill_declares_name_and_description | pass 2026-08-01 |
| S008 | `--audit new <type>` creates the dated report and refuses to overwrite an existing one; a fresh report has no live findings and leaves `--validate` clean | test_s008_audit.py TestAuditNew (3 tests) | pass 2026-08-01 |
| S008 | `--audit list` prints each finding with status and what references it, and exits non-zero while an open finding has neither step nor decision | TestAuditList (2 tests) | pass 2026-08-01 |
| S008 | INV-10 requires a finding id to carry its own report's name | TestFindingIds.test_finding_id_must_match_its_report | pass 2026-08-01 |
| S008 | the reviewer may write only under `project/audit/`: source, specs, plan, and step files are blocked; other agents are unaffected | TestReviewerWriteFence (4 tests) | pass 2026-08-01 |
| S008 | `adversarial_reviewer` is defined with Read and Write but never Edit; the `audit` skill declares name and description | TestDefinitions (2 tests) | pass 2026-08-01 |
| S008 | a scaffolded repo's first real `DEC-001` does not collide with the template's format example (regression: template guidance is not data) | test_s006_scaffold.py test_first_real_decision_does_not_collide_with_the_template_example (red observed before `strip_guidance`) | pass 2026-08-01 |
| S009 | the golden fails when a flag or `--step`/`--audit` operation is added, renamed, or removed | test_s009_surface.py TestGoldenSurface (red observed twice by tampering: added `--purge` flag, added `abandon` operation) | pass 2026-08-01 |
| S009 | refreshing the golden alone does not make the suite green: every flag and operation must appear in the specs CLI table, an operation only where its own mode is described | TestSurfaceIsDocumented.test_specs_describes_every_mode (red observed: `Lists differ: ['--purge'] != []`, `['--step abandon'] != []`) | pass 2026-08-01 |
| S009 | the same check runs against MANUAL.md once it exists | TestSurfaceIsDocumented.test_manual_describes_every_mode_once_it_exists | skipped 2026-08-01, activates in S011 |
| S009 | the DEC-012 ruleset identity guard is present and not duplicated | TestRulesetIdentity.test_identity_guard_is_present (comparison itself lives in test_s006_scaffold.py) | pass 2026-08-01 |
| S010 | `plugin.json` carries a kebab-case name matching the documented `/moltke:<skill>` namespace, an explicit semantic version (DEC-002), a description, and no path field pointing at a missing component | test_s010_plugin.py TestManifest (4 tests) | pass 2026-08-01 |
| S010 | every skill directory names itself in frontmatter so it resolves as `/moltke:<skill>`; skills are not hidden inside `.claude-plugin/`; the reviewer agent is discoverable; hooks call the checker through `${CLAUDE_PLUGIN_ROOT}` and the checker is executable | TestComponentsAreDiscoverable (4 tests) | pass 2026-08-01 |
| S010 | the marketplace entry resolves to this plugin and does not contradict the manifest version | TestMarketplace (2 tests) | pass 2026-08-01 |
| S010 | `claude plugin validate --strict` passes on the marketplace manifest, and on the plugin manifest, skills, agents, and hooks | TestClaudeValidates (skips without the claude CLI; run here: marketplace "Validation passed", plugin copy passed apart from the DEC-020 root-CLAUDE.md warning) | pass 2026-08-01 |
| S011 | MANUAL.md describes every flag and every `--step`/`--audit` operation, each operation on a line naming its own mode | test_s009_surface.py TestSurfaceIsDocumented.test_manual_describes_every_mode_once_it_exists (no longer skipped; red observed by deleting two modes: `Lists differ: ['--decline', '--audit list'] != []`) | pass 2026-08-02 |
| S011 | README states the exact build, test, validate, and golden-refresh commands, and that no environment variables are read | manual verification against the code: `grep environ bin/moltke.py` returns nothing; every command in README run here | pass 2026-08-02 |
| S011 | MANUAL records the known issues as claims about code: plugin ships repo state, mechanical docs gate, reviewer fence depends on `agent_type`, INV-7/8 abstain without git, live hooks unverified | manual verification against DEC-020, `--step done` stamp check, `mode_pre_write`, `inv_7`/`inv_8` abstain branches, and S012 being open | pass 2026-08-02 |
| S013 | `--scaffold` creates `adocs/` and never `project/` (DEC-021) | test_s013_adocs.py TestScaffoldNamesAdocs.test_scaffold_creates_adocs_and_never_project (red observed by pointing SCAFFOLD_DIRS back at `project/`: `AssertionError: True is not false : scaffold created project/`) | pass 2026-08-02 |
| S013 | no live documentation file still names `project/`; history is exempt | test_s013_adocs.py TestNoStaleProjectPaths.test_live_docs_name_adocs (red observed by inserting one stale line into README: `Lists differ: ['README.md:2: stale ref to project/status.md'] != []`) | pass 2026-08-02 |
| S013 | every path and user-facing message in `bin/moltke.py` reads `adocs/`, through the single `DOCS` constant | `grep -n project bin/moltke.py` returns only the DEC-021 comment on line 17; full suite green on the renamed fixtures | pass 2026-08-02 |
| S013 | `AGENTS.md` and `templates/AGENTS.md` stay byte-identical across the rename | test_s006_scaffold.py test_template_agents_matches_repo_agents + `cmp AGENTS.md templates/AGENTS.md` | pass 2026-08-02 |
| S013 | `worklog.md` and `decisions.md` survive the move byte-identical (INV-8 has no HEAD baseline at the new path for one commit and abstains) | md5sum before and after the move, both `fc2750fa…` and `2ed9d790…`; abstain branch confirmed by `git show HEAD:adocs/worklog.md` returning 128 in a throwaway clone | pass 2026-08-02 |
| S013 | INV-7 reads the moved `plan_done/` files as additions, the one legal change | `git status --porcelain -- adocs/plan_done` in a throwaway clone reports `A` for every file, not `D` or `R` | pass 2026-08-02 |
| S012 | `claude plugin marketplace add` and `claude plugin install moltke@moltke` succeed, and `/moltke:init`, `/moltke:step`, `/moltke:audit` resolve | manual, two machines (Max, 2026-08-06); `claude plugin list` reports `moltke@moltke`; all three skills listed by the session | pass 2026-08-06 |
| S012 | all five hook events fire in a live session against the `adocs/` build | manual live probes 2026-08-06, one per event: SessionStart printed the stack and `Derived next step: S012`; UserPromptSubmit appended the prompt verbatim; PreToolUse refused a write into `plan_done/` and a step file at the repo root; PostToolUse surfaced `INV-3` on an unlisted step file without blocking the write; Stop refused a stale `status.md` naming the fix | pass 2026-08-06 |
| S012 | the `adversarial_reviewer` subagent is spawnable through the installed plugin | manual live spawn 2026-08-06 of `moltke:adversarial_reviewer`, which ran and reported back | pass 2026-08-06 |
| S012 | the reviewer write fence holds under a real subagent spawn | manual live probe 2026-08-06: the spawned reviewer wrote `PROBE_S012.md` to the repo root and reported `File created successfully`, with no hook denial; the path matches no step-file pattern and is not under `plan_done/`, so the fence was the only rule that could have blocked it | **fail 2026-08-06**; recorded as finding F02 in `adocs/audit/2026-08-06_adversarial.md`, fixed by S016 which re-probes with a live spawn |
| S012 | DEC-002 confirmed before the first push | `git remote -v` reports `origin git@github.com:macsimbodnar/moltke.git`; `origin/master` at `1064774` matched local HEAD before this session's commits | pass 2026-08-06 |
| S014 | `--log-prompt` in a marked repo whose `adocs/` is absent still records the prompt | test_s005_hooks.py TestLogPrompt.test_prompt_survives_a_missing_docs_directory (red observed before the `mkdir`: `AssertionError: False is not true : prompt lost: /var/folders/.../adocs/worklog.md was never created`) | pass 2026-08-06 |
| S014 | a swallowed worklog append is reported through `SessionStart` `additionalContext`, the only channel a zero exit reaches | test_s005_hooks.py TestLogPromptFailureIsLoud.test_failed_append_is_reported_at_session_start (red observed: `AssertionError: 'not appended' not found in 'moltke stack (plan_current/):\n  S003: active\nDerived next step: S002 ...'`); non-vacuous by construction — the test asserts the append really failed before asserting the report | pass 2026-08-06 |
| S014 | the report fires once and stops when the failure stops, so a fixed path needs no manual clearing | test_s005_hooks.py TestLogPromptFailureIsLoud.test_report_stops_once_the_failure_stops (red observed: same `'not appended' not found` on the first of the two calls) | pass 2026-08-06 |
| S014 | a healthy append reports nothing, so the failure line is not permanent noise | test_s005_hooks.py TestLogPromptFailureIsLoud.test_a_healthy_append_reports_nothing (control; green before and after the fix by design) | pass 2026-08-06 |
| S015 | with a prompt logged first, as every live session does, `--stop` still blocks a source change that has no recap | test_s005_hooks.py TestStop.test_blocks_source_change_without_recap, and every TestStop fixture now calls `log_prompt` before stopping (red observed: `AssertionError: 0 != 2 : ` with empty stderr — the gate was entirely silent) | pass 2026-08-06 |
| S015 | a recap heading after the last logged prompt unblocks | test_s005_hooks.py TestStop.test_recap_unblocks_source_change | pass 2026-08-06 |
| S015 | a recap older than the last logged prompt does not discharge the later turn | test_s005_hooks.py TestStop.test_recap_older_than_the_last_prompt_does_not_count (red observed: `AssertionError: 0 != 2`) | pass 2026-08-06 |
| S015 | a recap heading inside a fenced block is guidance, not a recap | test_s005_hooks.py TestStop.test_recap_inside_a_fenced_block_does_not_count (red observed: `AssertionError: 0 != 2`) | pass 2026-08-06 |
| S015 | a heading that reads as both, `recap … prompt`, counts as a recap — this repo's own S014 recap heading has that shape | test_s005_hooks.py TestStop.test_recap_about_prompts_is_still_a_recap (red observed against the ambiguous last-prompt-wins classification: `AssertionError: 2 != 0 : moltke: source changed but adocs/worklog.md has no recap heading after the last logged prompt…`) | pass 2026-08-06 |
| S015 | the recap gate abstains before the first commit, so a fresh scaffold does not block | test_s005_hooks.py TestStop.test_recap_gate_abstains_before_the_first_commit; non-vacuous — it asserts the same tree blocks once a commit exists (red observed while the abstain was missing: test_s006_scaffold.py `AssertionError: 2 != 0 : a freshly scaffolded repo must not block the first stop`) | pass 2026-08-06 |
