id:         S010
goal:       plugin manifest, marketplace entry, install verification on a second machine
accepts:    .claude-plugin/plugin.json carries an explicit version field; marketplace entry installs on a second machine; skills resolve as /max_agent_workflow:name; DEC-002 (public repository) confirmed with Max before the first push
touches:    .claude-plugin/, marketplace entry
decisions:  DEC-001, DEC-002
done:
